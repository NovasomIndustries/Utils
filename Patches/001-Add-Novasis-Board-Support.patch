diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/autologin.sh SDLBase/board/novasis/NOVAsomP/Init/bin/autologin.sh
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/autologin.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/bin/autologin.sh	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,2 @@
+#!/bin/sh
+/bin/login -f root
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/get_exec SDLBase/board/novasis/NOVAsomP/Init/bin/get_exec
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/get_exec	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/bin/get_exec	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,10 @@
+#!/bin/sh
+. /etc/sysconfig/system_vars
+if [ "$1" == "" ]; then
+        echo "Usage `basename $0` <filename>"
+        exit 0
+fi
+cd /tmp
+rm -f `basename ${1}`
+wget http://${REFERENCE_SERVER}/${1}
+chmod 777 `basename ${1}`
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/update_fs SDLBase/board/novasis/NOVAsomP/Init/bin/update_fs
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/update_fs	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/bin/update_fs	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,8 @@
+#!/bin/sh
+get_exec uInitrd       
+rm -rf /tmp/update_dir
+mkdir /tmp/update_dir
+mount /dev/mmcblk0p1 /tmp/update_dir
+cp /tmp/uInitrd /tmp/update_dir/.       
+umount /tmp/update_dir
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/update_kernel SDLBase/board/novasis/NOVAsomP/Init/bin/update_kernel
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/bin/update_kernel	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/bin/update_kernel	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,10 @@
+#!/bin/sh
+get_exec zImage
+get_exec novasomp.dtb
+rm -rf /tmp/update_dir
+mkdir /tmp/update_dir
+mount /dev/mmcblk0p1 /tmp/update_dir
+cp /tmp/zImage /tmp/update_dir/.       
+cp /tmp/novasomp.dtb /tmp/update_dir/.
+umount /tmp/update_dir
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/fstab SDLBase/board/novasis/NOVAsomP/Init/etc/fstab
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/fstab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/fstab	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,8 @@
+# <file system>	<mount pt>	<type>	<options>	<dump>	<pass>
+/dev/root	/		ext2	rw,noauto	0	1
+proc		/proc		proc	defaults	0	0
+devpts		/dev/pts	devpts	defaults,gid=5,mode=620	0	0
+tmpfs		/dev/shm	tmpfs	mode=0777	0	0
+tmpfs		/tmp		tmpfs	mode=1777	0	0
+tmpfs		/run		tmpfs	mode=0755,nosuid,nodev	0	0
+sysfs		/sys		sysfs	defaults	0	0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/group SDLBase/board/novasis/NOVAsomP/Init/etc/group
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/group	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/group	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,26 @@
+root:x:0:
+daemon:x:1:
+bin:x:2:
+sys:x:3:
+adm:x:4:
+tty:x:5:
+disk:x:6:
+lp:x:7:
+mail:x:8:
+kmem:x:9:
+wheel:x:10:root
+cdrom:x:11:
+dialout:x:18:
+floppy:x:19:
+video:x:28:
+audio:x:29:
+tape:x:32:
+www-data:x:33:
+operator:x:37:
+utmp:x:43:
+plugdev:x:46:
+staff:x:50:
+lock:x:54:
+netdev:x:82:
+nogroup:x:99:
+users:x:100:
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_S49ntp SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_S49ntp
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_S49ntp	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_S49ntp	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,57 @@
+#! /bin/sh
+#
+# System-V init script for the openntp daemon
+#
+
+. /etc/sysconfig/network
+. /etc/sysconfig/wireless
+
+[ ! -f /usr/sbin/ntpd ] && return 0
+COUNT=0
+/sbin/hwclock -r
+case "$1" in
+  start)
+        echo "Starting ntpd"
+        IP=`ifconfig | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+        if [ "${WLAN_ENABLED}" = "Y" ]; then
+                while [ "$IP" == "" ]; do
+                        echo "Waiting for IP"
+                        sleep 1
+                        IP=`ifconfig ${WLAN_DEVICE} | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+                        let COUNT=$COUNT+1
+                        if [ ${COUNT} -ge 10 ]; then
+                                break
+                        fi
+                done
+                DEVICE=${WLAN_DEVICE}
+        fi
+        if [ "${ETH0_ENABLED}" = "Y" ]; then
+                while [ "$IP" == "" ]; do
+                        echo "Waiting for IP"
+                        sleep 1
+                        IP=`ifconfig ${NET_DEVICE} | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+                        let COUNT=$COUNT+1
+                        if [ ${COUNT} -ge 10 ]; then
+                                break
+                        fi
+                done
+                DEVICE=${NET_DEVICE}
+        fi
+        /usr/sbin/ntpd -g -I ${DEVICE}
+        /etc/init.d/hwclock_updater &
+        ;;
+  stop) echo "Stopping ntpd"
+        kill -9 `pidof ntpd`
+        ;;
+  restart) echo "Restarting ntpd"
+        $0 stop
+        sleep 1
+        $0 start
+        ;;
+  *) echo "Usage: $0 {start|stop|restart|reload|force-reload}" >&2
+        exit 1
+        ;;
+esac
+
+exit 0
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_services SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_services
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_services	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/afterlease_services	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,17 @@
+#!/bin/sh
+# called by usr/share/udhcpc/default.script after the dhcp have a valid ip
+
+. /etc/sysconfig/system_vars
+. /etc/sysconfig/network
+
+#PROGRESS=`cat /tmp/progress`
+for i in /etc/init.d/afterlease_S??* ;do
+	[ ! -f "$i" ] && continue
+	$i start
+done
+
+ifconfig | grep Bcast | awk '{print $2}' | sed 's/addr://g' > /tmp/my_ip
+ifconfig lo up
+sleep 1
+touch /tmp/system_ready
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/hwclock_updater SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/hwclock_updater
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/hwclock_updater	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/hwclock_updater	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,4 @@
+#!/bin/sh
+sleep 20
+/sbin/hwclock -w
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/rcK SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/rcK
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/rcK	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/rcK	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,27 @@
+#!/bin/sh
+
+
+# Stop all init scripts in /etc/init.d
+# executing them in reversed numerical order.
+#
+for i in $(ls -r /etc/init.d/S??*) ;do
+
+     # Ignore dangling symlinks (if any).
+     [ ! -f "$i" ] && continue
+
+     case "$i" in
+	*.sh)
+	    # Source shell script for speed.
+	    (
+		trap - INT QUIT TSTP
+		set stop
+		. $i
+	    )
+	    ;;
+	*)
+	    # No sh extension, so fork subprocess.
+	    $i stop
+	    ;;
+    esac
+done
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/rcS SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/rcS
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/rcS	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/rcS	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,52 @@
+#!/bin/sh
+
+mount /dev/mmcblk0p3 /mnt
+if [ -d /mnt/application_storage ]; then
+	cp -r /mnt/application_storage /tmp/.
+fi
+if [ -d /mnt/sysconfig ]; then
+	# If the file REINIT_VARS is present remove 
+	#  all saved and restart with default values
+	if [ -f /mnt/sysconfig/REINIT_VARS ]; then
+		cp -a /etc /mnt/sysconfig/.
+		cp -a /root /mnt/sysconfig/.
+		rm /mnt/sysconfig/REINIT_VARS
+	else
+		if [ -d /mnt/sysconfig/etc ]; then
+			cp -a /mnt/sysconfig/etc /
+		fi
+
+		if [ -d /mnt/sysconfig/root ]; then
+			cp -a /mnt/sysconfig/root /
+			cp -a /mnt/sysconfig/root /
+		fi
+	fi
+else
+	mkdir /mnt/sysconfig
+	cp -a /etc /mnt/sysconfig/.
+	cp -a /root /mnt/sysconfig/.
+fi
+umount /mnt
+
+# Start all init scripts in /etc/init.d
+# executing them in numerical order.
+#
+for i in /etc/init.d/S??* ;do
+	# Ignore dangling symlinks (if any).
+	[ ! -f "$i" ] && continue
+
+	case "$i" in
+		*.sh)
+			# Source shell script for speed.
+			(
+				trap - INT QUIT TSTP
+				set start
+				. $i
+			)
+			;;
+		*)
+			# No sh extension, so fork subprocess.
+			$i start
+			;;
+	esac
+done
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S01logging SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S01logging
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S01logging	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S01logging	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,40 @@
+#!/bin/sh
+#
+# Start logging
+#
+
+SYSLOGD_ARGS=-n
+KLOGD_ARGS=-n
+[ -r /etc/default/logging ] && . /etc/default/logging
+
+start() {
+	printf "Starting logging: "
+	start-stop-daemon -b -S -q -m -p /var/run/syslogd.pid --exec /sbin/syslogd -- $SYSLOGD_ARGS
+	start-stop-daemon -b -S -q -m -p /var/run/klogd.pid --exec /sbin/klogd -- $KLOGD_ARGS
+	echo "OK"
+}
+
+stop() {
+	printf "Stopping logging: "
+	start-stop-daemon -K -q -p /var/run/syslogd.pid
+	start-stop-daemon -K -q -p /var/run/klogd.pid
+	echo "OK"
+}
+
+case "$1" in
+  start)
+	start
+	;;
+  stop)
+	stop
+	;;
+  restart|reload)
+	stop
+	start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S20urandom SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S20urandom
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S20urandom	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S20urandom	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,51 @@
+#! /bin/sh
+#
+# urandom	This script saves the random seed between reboots.
+#		It is called from the boot, halt and reboot scripts.
+#
+# Version:	@(#)urandom  1.33  22-Jun-1998  miquels@cistron.nl
+#
+
+[ -c /dev/urandom ] || exit 0
+#. /etc/default/rcS
+
+case "$1" in
+	start|"")
+		# check for read only file system
+		if ! touch /etc/random-seed 2>/dev/null
+		then
+			echo "read-only file system detected...done"
+			exit
+		fi
+		if [ "$VERBOSE" != no ]
+		then
+			printf "Initializing random number generator... "
+		fi
+		# Load and then save 512 bytes,
+		# which is the size of the entropy pool
+		cat /etc/random-seed >/dev/urandom
+		rm -f /etc/random-seed
+		umask 077
+		dd if=/dev/urandom of=/etc/random-seed count=1 \
+			>/dev/null 2>&1 || echo "urandom start: failed."
+		umask 022
+		[ "$VERBOSE" != no ] && echo "done."
+		;;
+	stop)
+		if ! touch /etc/random-seed 2>/dev/null
+                then
+                        exit
+                fi
+		# Carry a random seed from shut-down to start-up;
+		# see documentation in linux/drivers/char/random.c
+		[ "$VERBOSE" != no ] && printf "Saving random seed... "
+		umask 077
+		dd if=/dev/urandom of=/etc/random-seed count=1 \
+			>/dev/null 2>&1 || echo "urandom stop: failed."
+		[ "$VERBOSE" != no ] && echo "done."
+		;;
+	*)
+		echo "Usage: urandom {start|stop}" >&2
+		exit 1
+		;;
+esac
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S40network SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S40network
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S40network	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S40network	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,56 @@
+#!/bin/sh
+#
+# Start the network....
+#
+
+. /etc/sysconfig/system_vars
+. /etc/sysconfig/network
+
+[ "${ETH0_ENABLED}" = "N" ] && exit 0
+
+MAC_FROM_CMDLINE="00:4f:9f:01:00:0e"
+for MACADDR in `cat /proc/cmdline` ; do
+        echo $MACADDR | grep mac_addr > /dev/null
+        [ "$?" == "0" ] && MAC_FROM_CMDLINE=`echo "${MACADDR}" | sed 's/mac_addr=//g'`
+done
+
+if [ -f /etc/sysconfig/mac_addr ];  then
+        . /etc/sysconfig/mac_addr
+fi
+
+case "$1" in
+  start)
+ 	echo "Starting network..."
+	if [ "$NET_USE_DHCP" == "Y" ]; then
+		ifconfig ${NET_DEVICE} hw ether ${MAC_FROM_CMDLINE} up
+		udhcpc -i ${NET_DEVICE} &
+	else
+		echo "search ${NET_SUFFIX}" > /etc/resolv.conf
+		echo "nameserver ${NET_DNS_LIST}" >> /etc/resolv.conf
+		ifconfig ${NET_DEVICE} ${NET_IP_ADDRESS} netmask ${NET_MASK} hw ether ${MAC_FROM_CMDLINE} up >/dev/null 2>&1
+		route add default gw ${NET_GATEWAY} dev ${NET_DEVICE} >/dev/null 2>&1
+		echo "${NET_IP_ADDRESS}" > /tmp/my_ip
+	fi
+	if [ -f /usr/sbin/telnetd ]; then
+		telnetd
+	fi
+	if [ -f /usr/sbin/inetd ]; then
+		inetd
+	fi
+	;;
+  stop)
+	echo -n "Stopping network..."
+	/sbin/ifdown -a
+	kill -9 `pidof telnetd`
+	;;
+  restart|reload)
+	"$0" stop
+	"$0" start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S49ntp SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S49ntp
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S49ntp	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S49ntp	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,6 @@
+#! /bin/sh
+#
+# Empty, used after network is up
+#
+
+exit 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S50sshd SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S50sshd
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S50sshd	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S50sshd	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,57 @@
+#!/bin/sh
+#
+# sshd        Starts sshd.
+#
+
+# Make sure the ssh-keygen progam exists
+[ -f /usr/bin/ssh-keygen ] || exit 0
+
+mount /dev/mmcblk0p3 /mnt
+if [ -d /mnt/sysconfig/ssh ]; then
+	cp -r /mnt/sysconfig/ss* /etc/.
+else
+	# Create any missing keys
+	/usr/bin/ssh-keygen -A
+	if [ ! -d /mnt/sysconfig ]; then
+		mkdir /mnt/sysconfig
+	fi
+	cp -r /etc/ss* /mnt/sysconfig/.
+fi
+umount /mnt
+
+umask 077
+
+start() {
+	echo -n "Starting sshd: "
+	/usr/sbin/sshd
+	touch /var/lock/sshd
+	echo "OK"
+}
+stop() {
+	echo -n "Stopping sshd: "
+	killall sshd
+	rm -f /var/lock/sshd
+	echo "OK"
+}
+restart() {
+	stop
+	start
+}
+
+case "$1" in
+  start)
+	start
+	;;
+  stop)
+	stop
+	;;
+  restart|reload)
+	restart
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S50telnet SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S50telnet
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S50telnet	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S50telnet	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,36 @@
+#!/bin/sh
+#
+# Start telnet....
+#
+
+start() {
+      printf "Starting telnetd: "
+      start-stop-daemon -S -q -m -b -p /var/run/telnetd.pid \
+			-x /usr/sbin/telnetd -- -F
+      [ $? = 0 ] && echo "OK" || echo "FAIL"
+}
+
+stop() {
+	printf "Stopping telnetd: "
+	start-stop-daemon -K -q -p /var/run/telnetd.pid \
+			  -x /usr/sbin/telnetd
+	[ $? = 0 ] && echo "OK" || echo "FAIL"
+}
+
+case "$1" in
+    start)
+	start
+	;;
+    stop)
+	stop
+	;;
+    restart|reload)
+	stop
+	start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S97mysqld SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S97mysqld
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/init.d/S97mysqld	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/init.d/S97mysqld	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,38 @@
+#!/bin/sh
+[ ! -f /usr/bin/mysql ] && exit 0
+
+case "$1" in
+	start)
+		if [ ! -d /tmp/mysql ] ; then
+			echo "Creating MySQL system tables..."
+			mysql_install_db --user=mysql --ldata=/tmp/mysql
+		fi
+
+		# We don't use start-stop-daemon because mysqld has
+		# its own wrapper script.
+		echo -n "Starting mysql..."
+		/usr/bin/mysqld_safe --pid-file=/var/run/mysqld.pid  --datadir=/tmp/mysql &
+		usleep 900000
+mysql << _EOF_
+GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' IDENTIFIED BY 'novasis';
+quit
+_EOF_
+
+
+		echo "done."
+		;;
+	stop)
+		echo -n "Stopping mysql..."
+		if test -f /var/run/mysqld.pid ; then
+			kill `cat /var/run/mysqld.pid`
+		fi
+		echo "done."
+		;;
+	restart)
+		$0 stop
+		$0 start
+		;;
+	*)
+		echo "Usage: /etc/init.d/mysqld {start|stop|restart}"
+		;;
+esac
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/inittab SDLBase/board/novasis/NOVAsomP/Init/etc/inittab
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/inittab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/inittab	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,35 @@
+# /etc/inittab
+#
+# Copyright (C) 2001 Erik Andersen <andersen@codepoet.org>
+#
+# Note: BusyBox init doesn't support runlevels.  The runlevels field is
+# completely ignored by BusyBox init. If you want runlevels, use
+# sysvinit.
+#
+# Format for each entry: <id>:<runlevels>:<action>:<process>
+#
+# id        == tty to run on, or empty for /dev/console
+# runlevels == ignored
+# action    == one of sysinit, respawn, askfirst, wait, and once
+# process   == program to run
+
+# Startup the system
+::sysinit:/bin/mount -t proc proc /proc
+::sysinit:/bin/mount -o remount,rw /
+::sysinit:/bin/mkdir -p /dev/pts
+::sysinit:/bin/mkdir -p /dev/shm
+::sysinit:/bin/mount -a
+::sysinit:/bin/hostname -F /etc/hostname
+# now run any rc scripts
+::sysinit:/etc/init.d/rcS
+
+# Put a getty on the serial port
+ttymxc2::respawn:/sbin/getty -L -l /bin/autologin.sh -n ttymxc2 115200 vt100 # GENERIC_SERIAL
+
+# Stuff to do for the 3-finger salute
+#::ctrlaltdel:/sbin/reboot
+
+# Stuff to do before rebooting
+::shutdown:/etc/init.d/rcK
+::shutdown:/sbin/swapoff -a
+::shutdown:/bin/umount -a -r
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/issue SDLBase/board/novasis/NOVAsomP/Init/etc/issue
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/issue	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/issue	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1 @@
+Welcome
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/mtab SDLBase/board/novasis/NOVAsomP/Init/etc/mtab
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/mtab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/mtab	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,30 @@
+sysfs /sys sysfs rw,nosuid,nodev,noexec,relatime 0 0
+proc /proc proc rw,nosuid,nodev,noexec,relatime 0 0
+udev /dev devtmpfs rw,nosuid,relatime,size=4971252k,nr_inodes=1242813,mode=755 0 0
+devpts /dev/pts devpts rw,nosuid,noexec,relatime,gid=5,mode=620,ptmxmode=000 0 0
+tmpfs /run tmpfs rw,nosuid,noexec,relatime,size=998148k,mode=755 0 0
+/dev/sda1 / ext4 rw,relatime,errors=remount-ro,data=ordered 0 0
+securityfs /sys/kernel/security securityfs rw,nosuid,nodev,noexec,relatime 0 0
+tmpfs /dev/shm tmpfs rw,nosuid,nodev 0 0
+tmpfs /run/lock tmpfs rw,nosuid,nodev,noexec,relatime,size=5120k 0 0
+tmpfs /sys/fs/cgroup tmpfs ro,nosuid,nodev,noexec,mode=755 0 0
+cgroup /sys/fs/cgroup/systemd cgroup rw,nosuid,nodev,noexec,relatime,xattr,release_agent=/lib/systemd/systemd-cgroups-agent,name=systemd 0 0
+pstore /sys/fs/pstore pstore rw,nosuid,nodev,noexec,relatime 0 0
+cgroup /sys/fs/cgroup/net_cls,net_prio cgroup rw,nosuid,nodev,noexec,relatime,net_cls,net_prio 0 0
+cgroup /sys/fs/cgroup/pids cgroup rw,nosuid,nodev,noexec,relatime,pids 0 0
+cgroup /sys/fs/cgroup/cpu,cpuacct cgroup rw,nosuid,nodev,noexec,relatime,cpu,cpuacct 0 0
+cgroup /sys/fs/cgroup/devices cgroup rw,nosuid,nodev,noexec,relatime,devices 0 0
+cgroup /sys/fs/cgroup/hugetlb cgroup rw,nosuid,nodev,noexec,relatime,hugetlb 0 0
+cgroup /sys/fs/cgroup/perf_event cgroup rw,nosuid,nodev,noexec,relatime,perf_event 0 0
+cgroup /sys/fs/cgroup/freezer cgroup rw,nosuid,nodev,noexec,relatime,freezer 0 0
+cgroup /sys/fs/cgroup/cpuset cgroup rw,nosuid,nodev,noexec,relatime,cpuset 0 0
+cgroup /sys/fs/cgroup/memory cgroup rw,nosuid,nodev,noexec,relatime,memory 0 0
+cgroup /sys/fs/cgroup/blkio cgroup rw,nosuid,nodev,noexec,relatime,blkio 0 0
+systemd-1 /proc/sys/fs/binfmt_misc autofs rw,relatime,fd=26,pgrp=1,timeout=0,minproto=5,maxproto=5,direct 0 0
+debugfs /sys/kernel/debug debugfs rw,relatime 0 0
+hugetlbfs /dev/hugepages hugetlbfs rw,relatime 0 0
+mqueue /dev/mqueue mqueue rw,relatime 0 0
+fusectl /sys/fs/fuse/connections fusectl rw,relatime 0 0
+UShared /media/sf_UShared vboxsf rw,nodev,relatime 0 0
+tmpfs /run/user/1000 tmpfs rw,nosuid,nodev,relatime,size=998148k,mode=700,uid=1000,gid=1000 0 0
+gvfsd-fuse /run/user/1000/gvfs fuse.gvfsd-fuse rw,nosuid,nodev,relatime,user_id=1000,group_id=1000 0 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/network/if-pre-up.d/wait_iface SDLBase/board/novasis/NOVAsomP/Init/etc/network/if-pre-up.d/wait_iface
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/network/if-pre-up.d/wait_iface	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/network/if-pre-up.d/wait_iface	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,21 @@
+#!/bin/sh
+
+# In case we have a slow-to-appear interface (e.g. eth-over-USB),
+# and we need to configure it, wait until it appears, but not too
+# long either. IF_WAIT_DELAY is in seconds.
+
+if [ "${IF_WAIT_DELAY}" -a ! -e "/sys/class/net/${IFACE}" ]; then
+    printf "Waiting for interface %s to appear" "${IFACE}"
+    while [ ${IF_WAIT_DELAY} -gt 0 ]; do
+        if [ -e "/sys/class/net/${IFACE}" ]; then
+            printf "\n"
+            exit 0
+        fi
+        sleep 1
+        printf "."
+        : $((IF_WAIT_DELAY -= 1))
+    done
+    printf " timeout!\n"
+    exit 1
+fi
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/network/interfaces SDLBase/board/novasis/NOVAsomP/Init/etc/network/interfaces
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/network/interfaces	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/network/interfaces	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,9 @@
+# interface file auto-generated by buildroot
+
+auto lo
+iface lo inet loopback
+
+auto eth0
+iface eth0 inet dhcp
+	pre-up /etc/network/nfs_check
+	wait-delay 15
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/network/nfs_check SDLBase/board/novasis/NOVAsomP/Init/etc/network/nfs_check
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/network/nfs_check	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/network/nfs_check	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,20 @@
+#!/bin/sh
+
+# This allows NFS booting to work while also being able to configure
+# the network interface via DHCP when not NFS booting.  Otherwise, a
+# NFS booted system will likely hang during DHCP configuration.
+
+# Attempting to configure the network interface used for NFS will
+# initially bring that network down.  Since the root filesystem is
+# accessed over this network, the system hangs.
+
+# This script is run by ifup and will attempt to detect if a NFS root
+# mount uses the interface to be configured (IFACE), and if so does
+# not configure it.  This should allow the same build to be disk/flash
+# booted or NFS booted.
+
+nfsip=`sed -n '/^[^ ]*:.* \/ nfs.*[ ,]addr=\([0-9.]\+\).*/s//\1/p' /proc/mounts`
+if [ -n "$nfsip" ] && ip route get to "$nfsip" | grep -q "dev $IFACE"; then
+	echo Skipping $IFACE, used for NFS from $nfsip
+	exit 1
+fi
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/nsswitch.conf SDLBase/board/novasis/NOVAsomP/Init/etc/nsswitch.conf
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/nsswitch.conf	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/nsswitch.conf	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,13 @@
+# /etc/nsswitch.conf
+
+passwd:         files
+group:          files
+shadow:         files
+
+hosts:          files dns
+networks:       files dns
+
+protocols:      files
+services:       files
+ethers:         files
+rpc:            files
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/os-release SDLBase/board/novasis/NOVAsomP/Init/etc/os-release
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/os-release	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/os-release	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,5 @@
+NAME=Buildroot
+VERSION=2016.05
+ID=buildroot
+VERSION_ID=2016.05
+PRETTY_NAME="Buildroot 2016.05"
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/passwd SDLBase/board/novasis/NOVAsomP/Init/etc/passwd
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/passwd	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/passwd	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,9 @@
+root:x:0:0:root:/root:/bin/sh
+daemon:x:1:1:daemon:/usr/sbin:/bin/false
+bin:x:2:2:bin:/bin:/bin/false
+sys:x:3:3:sys:/dev:/bin/false
+sync:x:4:100:sync:/bin:/bin/sync
+mail:x:8:8:mail:/var/spool/mail:/bin/false
+www-data:x:33:33:www-data:/var/www:/bin/false
+operator:x:37:37:Operator:/var:/bin/false
+nobody:x:99:99:nobody:/home:/bin/false
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/profile SDLBase/board/novasis/NOVAsomP/Init/etc/profile
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/profile	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/profile	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,20 @@
+export PATH=/bin:/sbin:/usr/bin:/usr/sbin
+
+if [ "$PS1" ]; then
+	if [ "`id -u`" -eq 0 ]; then
+		export PS1='# '
+	else
+		export PS1='$ '
+	fi
+fi
+
+export PAGER='/bin/more '
+export EDITOR='/bin/vi'
+
+# Source configuration files from /etc/profile.d
+for i in /etc/profile.d/*.sh ; do
+	if [ -r "$i" ]; then
+		. $i
+	fi
+	unset i
+done
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/profile.d/umask.sh SDLBase/board/novasis/NOVAsomP/Init/etc/profile.d/umask.sh
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/profile.d/umask.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/profile.d/umask.sh	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1 @@
+umask 022
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/protocols SDLBase/board/novasis/NOVAsomP/Init/etc/protocols
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/protocols	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/protocols	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,61 @@
+# Internet (IP) protocols
+#
+# Updated from http://www.iana.org/assignments/protocol-numbers and other
+# sources.
+
+ip	0	IP		# internet protocol, pseudo protocol number
+hopopt	0	HOPOPT		# IPv6 Hop-by-Hop Option [RFC1883]
+icmp	1	ICMP		# internet control message protocol
+igmp	2	IGMP		# Internet Group Management
+ggp	3	GGP		# gateway-gateway protocol
+ipencap	4	IP-ENCAP	# IP encapsulated in IP (officially ``IP'')
+st	5	ST		# ST datagram mode
+tcp	6	TCP		# transmission control protocol
+egp	8	EGP		# exterior gateway protocol
+igp	9	IGP		# any private interior gateway (Cisco)
+pup	12	PUP		# PARC universal packet protocol
+udp	17	UDP		# user datagram protocol
+hmp	20	HMP		# host monitoring protocol
+xns-idp	22	XNS-IDP		# Xerox NS IDP
+rdp	27	RDP		# "reliable datagram" protocol
+iso-tp4	29	ISO-TP4		# ISO Transport Protocol class 4 [RFC905]
+dccp	33	DCCP		# Datagram Congestion Control Prot. [RFC4340]
+xtp	36	XTP		# Xpress Transfer Protocol
+ddp	37	DDP		# Datagram Delivery Protocol
+idpr-cmtp 38	IDPR-CMTP	# IDPR Control Message Transport
+ipv6	41	IPv6		# Internet Protocol, version 6
+ipv6-route 43	IPv6-Route	# Routing Header for IPv6
+ipv6-frag 44	IPv6-Frag	# Fragment Header for IPv6
+idrp	45	IDRP		# Inter-Domain Routing Protocol
+rsvp	46	RSVP		# Reservation Protocol
+gre	47	GRE		# General Routing Encapsulation
+esp	50	IPSEC-ESP	# Encap Security Payload [RFC2406]
+ah	51	IPSEC-AH	# Authentication Header [RFC2402]
+skip	57	SKIP		# SKIP
+ipv6-icmp 58	IPv6-ICMP	# ICMP for IPv6
+ipv6-nonxt 59	IPv6-NoNxt	# No Next Header for IPv6
+ipv6-opts 60	IPv6-Opts	# Destination Options for IPv6
+rspf	73	RSPF CPHB	# Radio Shortest Path First (officially CPHB)
+vmtp	81	VMTP		# Versatile Message Transport
+eigrp	88	EIGRP		# Enhanced Interior Routing Protocol (Cisco)
+ospf	89	OSPFIGP		# Open Shortest Path First IGP
+ax.25	93	AX.25		# AX.25 frames
+ipip	94	IPIP		# IP-within-IP Encapsulation Protocol
+etherip	97	ETHERIP		# Ethernet-within-IP Encapsulation [RFC3378]
+encap	98	ENCAP		# Yet Another IP encapsulation [RFC1241]
+#	99			# any private encryption scheme
+pim	103	PIM		# Protocol Independent Multicast
+ipcomp	108	IPCOMP		# IP Payload Compression Protocol
+vrrp	112	VRRP		# Virtual Router Redundancy Protocol [RFC5798]
+l2tp	115	L2TP		# Layer Two Tunneling Protocol [RFC2661]
+isis	124	ISIS		# IS-IS over IPv4
+sctp	132	SCTP		# Stream Control Transmission Protocol
+fc	133	FC		# Fibre Channel
+mobility-header 135 Mobility-Header # Mobility Support for IPv6 [RFC3775]
+udplite	136	UDPLite		# UDP-Lite [RFC3828]
+mpls-in-ip 137	MPLS-in-IP	# MPLS-in-IP [RFC4023]
+manet	138			# MANET Protocols [RFC5498]
+hip	139	HIP		# Host Identity Protocol
+shim6	140	Shim6		# Shim6 Protocol [RFC5533]
+wesp	141	WESP		# Wrapped Encapsulating Security Payload
+rohc	142	ROHC		# Robust Header Compression
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/services SDLBase/board/novasis/NOVAsomP/Init/etc/services
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/services	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/services	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,302 @@
+# /etc/services:
+# $Id: services,v 1.1 2004/10/09 02:49:18 andersen Exp $
+#
+# Network services, Internet style
+#
+# Note that it is presently the policy of IANA to assign a single well-known
+# port number for both TCP and UDP; hence, most entries here have two entries
+# even if the protocol doesn't support UDP operations.
+# Updated from RFC 1700, ``Assigned Numbers'' (October 1994).  Not all ports
+# are included, only the more common ones.
+
+tcpmux		1/tcp				# TCP port service multiplexer
+echo		7/tcp
+echo		7/udp
+discard		9/tcp		sink null
+discard		9/udp		sink null
+systat		11/tcp		users
+daytime		13/tcp
+daytime		13/udp
+netstat		15/tcp
+qotd		17/tcp		quote
+msp		18/tcp				# message send protocol
+msp		18/udp				# message send protocol
+chargen		19/tcp		ttytst source
+chargen		19/udp		ttytst source
+ftp-data	20/tcp
+ftp		21/tcp
+fsp		21/udp		fspd
+ssh		22/tcp				# SSH Remote Login Protocol
+ssh		22/udp				# SSH Remote Login Protocol
+telnet		23/tcp
+# 24 - private
+smtp		25/tcp		mail
+# 26 - unassigned
+time		37/tcp		timserver
+time		37/udp		timserver
+rlp		39/udp		resource	# resource location
+nameserver	42/tcp		name		# IEN 116
+whois		43/tcp		nicname
+re-mail-ck	50/tcp				# Remote Mail Checking Protocol
+re-mail-ck	50/udp				# Remote Mail Checking Protocol
+domain		53/tcp		nameserver	# name-domain server
+domain		53/udp		nameserver
+mtp		57/tcp				# deprecated
+bootps		67/tcp				# BOOTP server
+bootps		67/udp
+bootpc		68/tcp				# BOOTP client
+bootpc		68/udp
+tftp		69/udp
+gopher		70/tcp				# Internet Gopher
+gopher		70/udp
+rje		77/tcp		netrjs
+finger		79/tcp
+www		80/tcp		http		# WorldWideWeb HTTP
+www		80/udp				# HyperText Transfer Protocol
+link		87/tcp		ttylink
+kerberos	88/tcp		kerberos5 krb5	# Kerberos v5
+kerberos	88/udp		kerberos5 krb5	# Kerberos v5
+supdup		95/tcp
+# 100 - reserved
+hostnames	101/tcp		hostname	# usually from sri-nic
+iso-tsap	102/tcp		tsap		# part of ISODE.
+csnet-ns	105/tcp		cso-ns		# also used by CSO name server
+csnet-ns	105/udp		cso-ns
+# unfortunately the poppassd (Eudora) uses a port which has already
+# been assigned to a different service. We list the poppassd as an
+# alias here. This should work for programs asking for this service.
+# (due to a bug in inetd the 3com-tsmux line is disabled)
+#3com-tsmux	106/tcp		poppassd
+#3com-tsmux	106/udp		poppassd
+rtelnet		107/tcp				# Remote Telnet
+rtelnet		107/udp
+pop-2		109/tcp		postoffice	# POP version 2
+pop-2		109/udp
+pop-3		110/tcp				# POP version 3
+pop-3		110/udp
+sunrpc		111/tcp		portmapper	# RPC 4.0 portmapper TCP
+sunrpc		111/udp		portmapper	# RPC 4.0 portmapper UDP
+auth		113/tcp		authentication tap ident
+sftp		115/tcp
+uucp-path	117/tcp
+nntp		119/tcp		readnews untp	# USENET News Transfer Protocol
+ntp		123/tcp
+ntp		123/udp				# Network Time Protocol
+netbios-ns	137/tcp				# NETBIOS Name Service
+netbios-ns	137/udp
+netbios-dgm	138/tcp				# NETBIOS Datagram Service
+netbios-dgm	138/udp
+netbios-ssn	139/tcp				# NETBIOS session service
+netbios-ssn	139/udp
+imap2		143/tcp				# Interim Mail Access Proto v2
+imap2		143/udp
+snmp		161/udp				# Simple Net Mgmt Proto
+snmp-trap	162/udp		snmptrap	# Traps for SNMP
+cmip-man	163/tcp				# ISO mgmt over IP (CMOT)
+cmip-man	163/udp
+cmip-agent	164/tcp
+cmip-agent	164/udp
+xdmcp		177/tcp				# X Display Mgr. Control Proto
+xdmcp		177/udp
+nextstep	178/tcp		NeXTStep NextStep	# NeXTStep window
+nextstep	178/udp		NeXTStep NextStep	# server
+bgp		179/tcp				# Border Gateway Proto.
+bgp		179/udp
+prospero	191/tcp				# Cliff Neuman's Prospero
+prospero	191/udp
+irc		194/tcp				# Internet Relay Chat
+irc		194/udp
+smux		199/tcp				# SNMP Unix Multiplexer
+smux		199/udp
+at-rtmp		201/tcp				# AppleTalk routing
+at-rtmp		201/udp
+at-nbp		202/tcp				# AppleTalk name binding
+at-nbp		202/udp
+at-echo		204/tcp				# AppleTalk echo
+at-echo		204/udp
+at-zis		206/tcp				# AppleTalk zone information
+at-zis		206/udp
+qmtp		209/tcp				# The Quick Mail Transfer Protocol
+qmtp		209/udp				# The Quick Mail Transfer Protocol
+z3950		210/tcp		wais		# NISO Z39.50 database
+z3950		210/udp		wais
+ipx		213/tcp				# IPX
+ipx		213/udp
+imap3		220/tcp				# Interactive Mail Access
+imap3		220/udp				# Protocol v3
+ulistserv	372/tcp				# UNIX Listserv
+ulistserv	372/udp
+https		443/tcp				# MCom
+https		443/udp				# MCom
+snpp		444/tcp				# Simple Network Paging Protocol
+snpp		444/udp				# Simple Network Paging Protocol
+saft		487/tcp				# Simple Asynchronous File Transfer
+saft		487/udp				# Simple Asynchronous File Transfer
+npmp-local	610/tcp		dqs313_qmaster	# npmp-local / DQS
+npmp-local	610/udp		dqs313_qmaster	# npmp-local / DQS
+npmp-gui	611/tcp		dqs313_execd	# npmp-gui / DQS
+npmp-gui	611/udp		dqs313_execd	# npmp-gui / DQS
+hmmp-ind	612/tcp		dqs313_intercell# HMMP Indication / DQS
+hmmp-ind	612/udp		dqs313_intercell# HMMP Indication / DQS
+#
+# UNIX specific services
+#
+exec		512/tcp
+biff		512/udp		comsat
+login		513/tcp
+who		513/udp		whod
+shell		514/tcp		cmd		# no passwords used
+syslog		514/udp
+printer		515/tcp		spooler		# line printer spooler
+talk		517/udp
+ntalk		518/udp
+route		520/udp		router routed	# RIP
+timed		525/udp		timeserver
+tempo		526/tcp		newdate
+courier		530/tcp		rpc
+conference	531/tcp		chat
+netnews		532/tcp		readnews
+netwall		533/udp				# -for emergency broadcasts
+uucp		540/tcp		uucpd		# uucp daemon
+afpovertcp	548/tcp				# AFP over TCP
+afpovertcp	548/udp				# AFP over TCP
+remotefs	556/tcp		rfs_server rfs	# Brunhoff remote filesystem
+klogin		543/tcp				# Kerberized `rlogin' (v5)
+kshell		544/tcp		krcmd		# Kerberized `rsh' (v5)
+kerberos-adm	749/tcp				# Kerberos `kadmin' (v5)
+#
+webster		765/tcp				# Network dictionary
+webster		765/udp
+#
+# From ``Assigned Numbers'':
+#
+#> The Registered Ports are not controlled by the IANA and on most systems
+#> can be used by ordinary user processes or programs executed by ordinary
+#> users.
+#
+#> Ports are used in the TCP [45,106] to name the ends of logical
+#> connections which carry long term conversations.  For the purpose of
+#> providing services to unknown callers, a service contact port is
+#> defined.  This list specifies the port used by the server process as its
+#> contact port.  While the IANA can not control uses of these ports it
+#> does register or list uses of these ports as a convienence to the
+#> community.
+#
+nfsdstatus	1110/tcp
+nfsd-keepalive	1110/udp
+
+ingreslock	1524/tcp
+ingreslock	1524/udp
+prospero-np	1525/tcp			# Prospero non-privileged
+prospero-np	1525/udp
+datametrics	1645/tcp	old-radius	# datametrics / old radius entry
+datametrics	1645/udp	old-radius	# datametrics / old radius entry
+sa-msg-port	1646/tcp	old-radacct	# sa-msg-port / old radacct entry
+sa-msg-port	1646/udp	old-radacct	# sa-msg-port / old radacct entry
+radius		1812/tcp			# Radius
+radius		1812/udp			# Radius
+radacct		1813/tcp			# Radius Accounting
+radacct		1813/udp			# Radius Accounting
+nfsd		2049/tcp	nfs
+nfsd		2049/udp	nfs
+cvspserver	2401/tcp			# CVS client/server operations
+cvspserver	2401/udp			# CVS client/server operations
+mysql		3306/tcp			# MySQL
+mysql		3306/udp			# MySQL
+rfe		5002/tcp			# Radio Free Ethernet
+rfe		5002/udp			# Actually uses UDP only
+cfengine	5308/tcp			# CFengine
+cfengine	5308/udp			# CFengine
+bbs		7000/tcp			# BBS service
+#
+#
+# Kerberos (Project Athena/MIT) services
+# Note that these are for Kerberos v4, and are unofficial.  Sites running
+# v4 should uncomment these and comment out the v5 entries above.
+#
+kerberos4	750/udp		kerberos-iv kdc	# Kerberos (server) udp
+kerberos4	750/tcp		kerberos-iv kdc	# Kerberos (server) tcp
+kerberos_master	751/udp				# Kerberos authentication
+kerberos_master	751/tcp				# Kerberos authentication
+passwd_server	752/udp				# Kerberos passwd server
+krb_prop	754/tcp				# Kerberos slave propagation
+krbupdate	760/tcp		kreg		# Kerberos registration
+kpasswd		761/tcp		kpwd		# Kerberos "passwd"
+kpop		1109/tcp			# Pop with Kerberos
+knetd		2053/tcp			# Kerberos de-multiplexor
+zephyr-srv	2102/udp			# Zephyr server
+zephyr-clt	2103/udp			# Zephyr serv-hm connection
+zephyr-hm	2104/udp			# Zephyr hostmanager
+eklogin		2105/tcp			# Kerberos encrypted rlogin
+#
+# Unofficial but necessary (for NetBSD) services
+#
+supfilesrv	871/tcp				# SUP server
+supfiledbg	1127/tcp			# SUP debugging
+#
+# Datagram Delivery Protocol services
+#
+rtmp		1/ddp				# Routing Table Maintenance Protocol
+nbp		2/ddp				# Name Binding Protocol
+echo		4/ddp				# AppleTalk Echo Protocol
+zip		6/ddp				# Zone Information Protocol
+#
+# Services added for the Debian GNU/Linux distribution
+poppassd	106/tcp				# Eudora
+poppassd	106/udp				# Eudora
+mailq		174/tcp				# Mailer transport queue for Zmailer
+mailq		174/tcp				# Mailer transport queue for Zmailer
+omirr		808/tcp		omirrd		# online mirror
+omirr		808/udp		omirrd		# online mirror
+rmtcfg		1236/tcp			# Gracilis Packeten remote config server
+xtel		1313/tcp			# french minitel
+coda_opcons	1355/udp			# Coda opcons            (Coda fs)
+coda_venus	1363/udp			# Coda venus             (Coda fs)
+coda_auth	1357/udp			# Coda auth              (Coda fs)
+coda_udpsrv	1359/udp			# Coda udpsrv            (Coda fs)
+coda_filesrv	1361/udp			# Coda filesrv           (Coda fs)
+codacon		1423/tcp	venus.cmu	# Coda Console           (Coda fs)
+coda_aux1	1431/tcp			# coda auxiliary service (Coda fs)
+coda_aux1	1431/udp			# coda auxiliary service (Coda fs)
+coda_aux2	1433/tcp			# coda auxiliary service (Coda fs)
+coda_aux2	1433/udp			# coda auxiliary service (Coda fs)
+coda_aux3	1435/tcp			# coda auxiliary service (Coda fs)
+coda_aux3	1435/udp			# coda auxiliary service (Coda fs)
+cfinger		2003/tcp			# GNU Finger
+afbackup	2988/tcp			# Afbackup system
+afbackup	2988/udp			# Afbackup system
+icp		3130/tcp			# Internet Cache Protocol (Squid)
+icp		3130/udp			# Internet Cache Protocol (Squid)
+postgres	5432/tcp			# POSTGRES
+postgres	5432/udp			# POSTGRES
+fax		4557/tcp			# FAX transmission service        (old)
+hylafax		4559/tcp			# HylaFAX client-server protocol  (new)
+noclog		5354/tcp			# noclogd with TCP (nocol)
+noclog		5354/udp			# noclogd with UDP (nocol)
+hostmon		5355/tcp			# hostmon uses TCP (nocol)
+hostmon		5355/udp			# hostmon uses TCP (nocol)
+ircd		6667/tcp			# Internet Relay Chat
+ircd		6667/udp			# Internet Relay Chat
+webcache	8080/tcp			# WWW caching service
+webcache	8080/udp			# WWW caching service
+tproxy		8081/tcp			# Transparent Proxy
+tproxy		8081/udp			# Transparent Proxy
+mandelspawn	9359/udp	mandelbrot	# network mandelbrot
+amanda		10080/udp			# amanda backup services
+amandaidx	10082/tcp			# amanda backup services
+amidxtape	10083/tcp			# amanda backup services
+isdnlog		20011/tcp			# isdn logging system
+isdnlog		20011/udp			# isdn logging system
+vboxd		20012/tcp			# voice box system
+vboxd		20012/udp			# voice box system
+binkp           24554/tcp			# Binkley
+binkp           24554/udp			# Binkley
+asp		27374/tcp			# Address Search Protocol
+asp		27374/udp			# Address Search Protocol
+tfido           60177/tcp			# Ifmail
+tfido           60177/udp			# Ifmail
+fido            60179/tcp			# Ifmail
+fido            60179/udp			# Ifmail
+
+# Local services
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/shadow SDLBase/board/novasis/NOVAsomP/Init/etc/shadow
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/shadow	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/shadow	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,9 @@
+root::10933:0:99999:7:::
+daemon:*:10933:0:99999:7:::
+bin:*:10933:0:99999:7:::
+sys:*:10933:0:99999:7:::
+sync:*:10933:0:99999:7:::
+mail:*:10933:0:99999:7:::
+www-data:*:10933:0:99999:7:::
+operator:*:10933:0:99999:7:::
+nobody:*:10933:0:99999:7:::
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/sysconfig/network SDLBase/board/novasis/NOVAsomP/Init/etc/sysconfig/network
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/sysconfig/network	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/sysconfig/network	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,19 @@
+# Wired ethernet
+ETH0_ENABLED=Y
+NET_DEVICE=eth0
+NET_SUFFIX=novasis.it
+# Maybe N or Y
+# NET_USE_DHCP=N
+NET_USE_DHCP=Y
+#The following will be ignored if NET_USE_DHCP=N
+NET_IP_ADDRESS=192.168.10.51
+NET_BROADCAST=192.168.10.51
+NET_MASK=255.255.255.0
+NET_HOSTNAME=Novasom6_Ethernet
+NET_NETWORK=192.168.10.0
+NET_GATEWAY=192.168.10.1
+NET_DNS_LIST=192.168.10.200
+NET_DNS_DOMAIN=-
+NET_ALIAS=192.168.10.248
+NET_HTTPD_PORT=8080
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/sysconfig/system_vars SDLBase/board/novasis/NOVAsomP/Init/etc/sysconfig/system_vars
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/sysconfig/system_vars	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/sysconfig/system_vars	2017-01-10 15:04:22.681032694 +0100
@@ -0,0 +1 @@
+REFERENCE_SERVER=192.168.10.134
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/sysconfig/wireless SDLBase/board/novasis/NOVAsomP/Init/etc/sysconfig/wireless
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/etc/sysconfig/wireless	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/etc/sysconfig/wireless	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,16 @@
+#Wireless
+WLAN_ENABLED=N
+WLAN_USE_DHCP=N
+WLAN_DEVICE=wlan0
+WLAN_SUFFIX=novasis.it
+#The following will be ignored if WLAN_USE_DHCP=OFF
+WLAN_IP_ADDRESS=192.168.10.199
+WLAN_BROADCAST=192.168.10.199
+WLAN_MASK=255.255.255.0
+WLAN_HOSTNAME=Novasom6_Wlan
+WLAN_NETWORK=192.168.10.0
+WLAN_GATEWAY=192.168.10.1
+WLAN_DNS_LIST=192.168.10.200
+WLAN_DNS_DOMAIN=-
+WLAN_MAC=1E:72:49:ED:F7:54
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/root/.asoundrc SDLBase/board/novasis/NOVAsomP/Init/root/.asoundrc
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/root/.asoundrc	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/root/.asoundrc	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,38 @@
+pcm.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+ctl.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+pcm.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+ctl.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+pcm.copy {
+        type plug
+        slave {
+                pcm hw
+        }
+        route_policy copy
+}
+pcm.!default {
+        type hw
+        card 1
+        channels 2
+}
+ctl.!default {
+        type hw
+        card 1
+        channels 2
+}
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/root/.profile SDLBase/board/novasis/NOVAsomP/Init/root/.profile
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/root/.profile	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/root/.profile	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,10 @@
+#
+# .profile, started at boot from root user
+# 
+if [ -d /tmp/application_storage ]; then
+        if [ -f /tmp/application_storage/AutoRun.sh ]; then
+                chmod 777 /tmp/application_storage/AutoRun.sh
+                /tmp/application_storage/AutoRun.sh &
+        fi
+fi
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/Init/usr/share/udhcpc/default.script SDLBase/board/novasis/NOVAsomP/Init/usr/share/udhcpc/default.script
--- buildroot-2016.05/board/novasis/NOVAsomP/Init/usr/share/udhcpc/default.script	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/Init/usr/share/udhcpc/default.script	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,83 @@
+#!/bin/sh
+
+# udhcpc script edited by Tim Riker <Tim@Rikers.org>
+
+[ -z "$1" ] && echo "Error: should be called from udhcpc" && exit 1
+
+RESOLV_CONF="/etc/resolv.conf"
+[ -e $RESOLV_CONF ] || touch $RESOLV_CONF
+[ -n "$broadcast" ] && BROADCAST="broadcast $broadcast"
+[ -n "$subnet" ] && NETMASK="netmask $subnet"
+
+case "$1" in
+	deconfig)
+		/sbin/ifconfig $interface up
+		/sbin/ifconfig $interface 0.0.0.0
+
+		# drop info from this interface
+		# resolv.conf may be a symlink to /tmp/, so take care
+		TMPFILE=$(mktemp)
+		grep -vE "# $interface\$" $RESOLV_CONF > $TMPFILE
+		cat $TMPFILE > $RESOLV_CONF
+		rm -f $TMPFILE
+
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -k $interface
+		fi
+		;;
+
+	leasefail|nak)
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -wD $interface --no-chroot
+		fi
+		;;
+
+	renew|bound)
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -k $interface
+		fi
+		/sbin/ifconfig $interface $ip $BROADCAST $NETMASK
+
+		if [ -n "$router" ] ; then
+			echo "deleting routers"
+			while route del default gw 0.0.0.0 dev $interface 2> /dev/null; do
+				:
+			done
+
+			for i in $router ; do
+				route add default gw $i dev $interface
+			done
+		fi
+
+		# drop info from this interface
+		# resolv.conf may be a symlink to /tmp/, so take care
+		TMPFILE=$(mktemp)
+		grep -vE "# $interface\$" $RESOLV_CONF > $TMPFILE
+		cat $TMPFILE > $RESOLV_CONF
+		rm -f $TMPFILE
+
+		# prefer rfc3359 domain search list (option 119) if available
+		if [ -n "$search" ]; then
+			search_list=$search
+		elif [ -n "$domain" ]; then
+			search_list=$domain
+		fi
+
+		[ -n "$search_list" ] &&
+			echo "search $search_list # $interface" >> $RESOLV_CONF
+
+		for i in $dns ; do
+			echo adding dns $i
+			echo "nameserver $i # $interface" >> $RESOLV_CONF
+		done
+                /etc/init.d/afterlease_services
+		;;
+esac
+
+HOOK_DIR="$0.d"
+for hook in "${HOOK_DIR}/"*; do
+    [ -f "${hook}" -a -x "${hook}" ] || continue
+    "${hook}" "${@}"
+done
+
+exit 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/post-image.sh SDLBase/board/novasis/NOVAsomP/post-image.sh
--- buildroot-2016.05/board/novasis/NOVAsomP/post-image.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/post-image.sh	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,9 @@
+#!/bin/bash
+
+echo "###### Creating uInitrd for NOVAsomP ######"
+output/host/usr/bin/mkimage -A arm -O linux -T ramdisk -C none -n "NOVAsomP" -a 0x80000 -d output/images/rootfs.ext3.gz output/images/uInitrd
+pushd ../../Deploy
+rm uInitrd
+ln -s ${BASE_DIR}/images/uInitrd
+popd
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/pre-image.sh SDLBase/board/novasis/NOVAsomP/pre-image.sh
--- buildroot-2016.05/board/novasis/NOVAsomP/pre-image.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/pre-image.sh	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,9 @@
+#!/bin/bash
+
+echo "###### Customizing file system ######"
+echo "cp -r board/novasis/NOVAsomP/Init/* ${TARGET_DIR}"
+cp -r board/novasis/NOVAsomP/Init/* ${TARGET_DIR}
+chmod 777 ${TARGET_DIR}/bin/*
+chmod 777 ${TARGET_DIR}/etc/init.d/*
+sync
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomP/readme.txt SDLBase/board/novasis/NOVAsomP/readme.txt
--- buildroot-2016.05/board/novasis/NOVAsomP/readme.txt	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomP/readme.txt	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,29 @@
+***************************
+Novasis NOVAsomP board
+***************************
+
+This file documents the Buildroot support for the NOVAsomP board.
+
+Please read the Quick Start Guide [1] for an introduction to the board.
+
+Build
+=====
+
+Configure Buildroot for your i.MX6UL EVK board:
+
+  make novasis_novasomp_defconfig
+
+Build all components:
+
+  make
+
+You will find in ./output/images/ the following files:
+  - rootfs.ext2
+  - rootfs.tar
+  - uInitrd
+
+Enjoy!
+
+References
+==========
+[1] http://www.novasis.it/NOVAsomP.html
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/autologin.sh SDLBase/board/novasis/NOVAsomS/Init/bin/autologin.sh
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/autologin.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/bin/autologin.sh	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,2 @@
+#!/bin/sh
+/bin/login -f root
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/get_exec SDLBase/board/novasis/NOVAsomS/Init/bin/get_exec
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/get_exec	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/bin/get_exec	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,10 @@
+#!/bin/sh
+. /etc/sysconfig/system_vars
+if [ "$1" == "" ]; then
+        echo "Usage `basename $0` <filename>"
+        exit 0
+fi
+cd /tmp
+rm -f `basename ${1}`
+wget http://${REFERENCE_SERVER}/${1}
+chmod 777 `basename ${1}`
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/update_fs SDLBase/board/novasis/NOVAsomS/Init/bin/update_fs
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/update_fs	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/bin/update_fs	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,8 @@
+#!/bin/sh
+get_exec uInitrd       
+rm -rf /tmp/update_dir
+mkdir /tmp/update_dir
+mount /dev/mmcblk2p1 /tmp/update_dir
+cp /tmp/uInitrd /tmp/update_dir/.       
+umount /tmp/update_dir
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/update_kernel SDLBase/board/novasis/NOVAsomS/Init/bin/update_kernel
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/bin/update_kernel	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/bin/update_kernel	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,12 @@
+#!/bin/sh
+get_exec zImage
+get_exec imx6q-novasomS.dtb
+get_exec imx6sdl-novasomS.dtb
+rm -rf /tmp/update_dir
+mkdir /tmp/update_dir
+mount /dev/mmcblk2p1 /tmp/update_dir
+cp /tmp/zImage /tmp/update_dir/.       
+cp /tmp/imx6q-novasomS.dtb /tmp/update_dir/.
+cp /tmp/imx6sdl-novasomS.dtb /tmp/update_dir/.
+umount /tmp/update_dir
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/fstab SDLBase/board/novasis/NOVAsomS/Init/etc/fstab
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/fstab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/fstab	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,8 @@
+# <file system>	<mount pt>	<type>	<options>	<dump>	<pass>
+/dev/root	/		ext2	rw,noauto	0	1
+proc		/proc		proc	defaults	0	0
+devpts		/dev/pts	devpts	defaults,gid=5,mode=620	0	0
+tmpfs		/dev/shm	tmpfs	mode=0777	0	0
+tmpfs		/tmp		tmpfs	mode=1777	0	0
+tmpfs		/run		tmpfs	mode=0755,nosuid,nodev	0	0
+sysfs		/sys		sysfs	defaults	0	0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/group SDLBase/board/novasis/NOVAsomS/Init/etc/group
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/group	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/group	2017-01-10 14:12:55.005032694 +0100
@@ -0,0 +1,26 @@
+root:x:0:
+daemon:x:1:
+bin:x:2:
+sys:x:3:
+adm:x:4:
+tty:x:5:
+disk:x:6:
+lp:x:7:
+mail:x:8:
+kmem:x:9:
+wheel:x:10:root
+cdrom:x:11:
+dialout:x:18:
+floppy:x:19:
+video:x:28:
+audio:x:29:
+tape:x:32:
+www-data:x:33:
+operator:x:37:
+utmp:x:43:
+plugdev:x:46:
+staff:x:50:
+lock:x:54:
+netdev:x:82:
+nogroup:x:99:
+users:x:100:
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_S49ntp SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_S49ntp
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_S49ntp	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_S49ntp	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,57 @@
+#! /bin/sh
+#
+# System-V init script for the openntp daemon
+#
+
+. /etc/sysconfig/network
+. /etc/sysconfig/wireless
+
+[ ! -f /usr/sbin/ntpd ] && return 0
+COUNT=0
+/sbin/hwclock -r
+case "$1" in
+  start)
+        echo "Starting ntpd"
+        IP=`ifconfig | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+        if [ "${WLAN_ENABLED}" = "Y" ]; then
+                while [ "$IP" == "" ]; do
+                        echo "Waiting for IP"
+                        sleep 1
+                        IP=`ifconfig ${WLAN_DEVICE} | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+                        let COUNT=$COUNT+1
+                        if [ ${COUNT} -ge 10 ]; then
+                                break
+                        fi
+                done
+                DEVICE=${WLAN_DEVICE}
+        fi
+        if [ "${ETH0_ENABLED}" = "Y" ]; then
+                while [ "$IP" == "" ]; do
+                        echo "Waiting for IP"
+                        sleep 1
+                        IP=`ifconfig ${NET_DEVICE} | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+                        let COUNT=$COUNT+1
+                        if [ ${COUNT} -ge 10 ]; then
+                                break
+                        fi
+                done
+                DEVICE=${NET_DEVICE}
+        fi
+        /usr/sbin/ntpd -g -I ${DEVICE}
+        /etc/init.d/hwclock_updater &
+        ;;
+  stop) echo "Stopping ntpd"
+        kill -9 `pidof ntpd`
+        ;;
+  restart) echo "Restarting ntpd"
+        $0 stop
+        sleep 1
+        $0 start
+        ;;
+  *) echo "Usage: $0 {start|stop|restart|reload|force-reload}" >&2
+        exit 1
+        ;;
+esac
+
+exit 0
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_services SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_services
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_services	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/afterlease_services	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,17 @@
+#!/bin/sh
+# called by usr/share/udhcpc/default.script after the dhcp have a valid ip
+
+. /etc/sysconfig/system_vars
+. /etc/sysconfig/network
+
+#PROGRESS=`cat /tmp/progress`
+for i in /etc/init.d/afterlease_S??* ;do
+	[ ! -f "$i" ] && continue
+	$i start
+done
+
+ifconfig | grep Bcast | awk '{print $2}' | sed 's/addr://g' > /tmp/my_ip
+ifconfig lo up
+sleep 1
+touch /tmp/system_ready
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/hwclock_updater SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/hwclock_updater
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/hwclock_updater	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/hwclock_updater	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,4 @@
+#!/bin/sh
+sleep 20
+/sbin/hwclock -w
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/rcK SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/rcK
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/rcK	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/rcK	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,27 @@
+#!/bin/sh
+
+
+# Stop all init scripts in /etc/init.d
+# executing them in reversed numerical order.
+#
+for i in $(ls -r /etc/init.d/S??*) ;do
+
+     # Ignore dangling symlinks (if any).
+     [ ! -f "$i" ] && continue
+
+     case "$i" in
+	*.sh)
+	    # Source shell script for speed.
+	    (
+		trap - INT QUIT TSTP
+		set stop
+		. $i
+	    )
+	    ;;
+	*)
+	    # No sh extension, so fork subprocess.
+	    $i stop
+	    ;;
+    esac
+done
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/rcS SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/rcS
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/rcS	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/rcS	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,48 @@
+#!/bin/sh
+
+mount /dev/mmcblk2p3 /mnt
+if [ -d /mnt/sysconfig ]; then
+	# If the file REINIT_VARS is present remove all saved and restart with default values
+	if [ -f /mnt/sysconfig/REINIT_VARS ]; then
+		cp -a /etc /mnt/sysconfig/.
+		cp -a /root /mnt/sysconfig/.
+		rm /mnt/sysconfig/REINIT_VARS
+	else
+		if [ -d /mnt/sysconfig/etc ]; then
+			cp -a /mnt/sysconfig/etc /
+		fi
+
+		if [ -d /mnt/sysconfig/root ]; then
+			cp -a /mnt/sysconfig/root /
+			cp -a /mnt/sysconfig/root /
+		fi
+	fi
+else
+	mkdir /mnt/sysconfig
+	cp -a /etc /mnt/sysconfig/.
+	cp -a /root /mnt/sysconfig/.
+fi
+umount /mnt
+
+# Start all init scripts in /etc/init.d
+# executing them in numerical order.
+#
+for i in /etc/init.d/S??* ;do
+	# Ignore dangling symlinks (if any).
+	[ ! -f "$i" ] && continue
+
+	case "$i" in
+		*.sh)
+			# Source shell script for speed.
+			(
+				trap - INT QUIT TSTP
+				set start
+				. $i
+			)
+			;;
+		*)
+			# No sh extension, so fork subprocess.
+			$i start
+			;;
+	esac
+done
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S01logging SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S01logging
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S01logging	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S01logging	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,40 @@
+#!/bin/sh
+#
+# Start logging
+#
+
+SYSLOGD_ARGS=-n
+KLOGD_ARGS=-n
+[ -r /etc/default/logging ] && . /etc/default/logging
+
+start() {
+	printf "Starting logging: "
+	start-stop-daemon -b -S -q -m -p /var/run/syslogd.pid --exec /sbin/syslogd -- $SYSLOGD_ARGS
+	start-stop-daemon -b -S -q -m -p /var/run/klogd.pid --exec /sbin/klogd -- $KLOGD_ARGS
+	echo "OK"
+}
+
+stop() {
+	printf "Stopping logging: "
+	start-stop-daemon -K -q -p /var/run/syslogd.pid
+	start-stop-daemon -K -q -p /var/run/klogd.pid
+	echo "OK"
+}
+
+case "$1" in
+  start)
+	start
+	;;
+  stop)
+	stop
+	;;
+  restart|reload)
+	stop
+	start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S20urandom SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S20urandom
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S20urandom	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S20urandom	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,51 @@
+#! /bin/sh
+#
+# urandom	This script saves the random seed between reboots.
+#		It is called from the boot, halt and reboot scripts.
+#
+# Version:	@(#)urandom  1.33  22-Jun-1998  miquels@cistron.nl
+#
+
+[ -c /dev/urandom ] || exit 0
+#. /etc/default/rcS
+
+case "$1" in
+	start|"")
+		# check for read only file system
+		if ! touch /etc/random-seed 2>/dev/null
+		then
+			echo "read-only file system detected...done"
+			exit
+		fi
+		if [ "$VERBOSE" != no ]
+		then
+			printf "Initializing random number generator... "
+		fi
+		# Load and then save 512 bytes,
+		# which is the size of the entropy pool
+		cat /etc/random-seed >/dev/urandom
+		rm -f /etc/random-seed
+		umask 077
+		dd if=/dev/urandom of=/etc/random-seed count=1 \
+			>/dev/null 2>&1 || echo "urandom start: failed."
+		umask 022
+		[ "$VERBOSE" != no ] && echo "done."
+		;;
+	stop)
+		if ! touch /etc/random-seed 2>/dev/null
+                then
+                        exit
+                fi
+		# Carry a random seed from shut-down to start-up;
+		# see documentation in linux/drivers/char/random.c
+		[ "$VERBOSE" != no ] && printf "Saving random seed... "
+		umask 077
+		dd if=/dev/urandom of=/etc/random-seed count=1 \
+			>/dev/null 2>&1 || echo "urandom stop: failed."
+		[ "$VERBOSE" != no ] && echo "done."
+		;;
+	*)
+		echo "Usage: urandom {start|stop}" >&2
+		exit 1
+		;;
+esac
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S40network SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S40network
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S40network	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S40network	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,56 @@
+#!/bin/sh
+#
+# Start the network....
+#
+
+. /etc/sysconfig/system_vars
+. /etc/sysconfig/network
+
+[ "${ETH0_ENABLED}" = "N" ] && exit 0
+
+MAC_FROM_CMDLINE="00:4f:9f:01:00:0e"
+for MACADDR in `cat /proc/cmdline` ; do
+        echo $MACADDR | grep mac_addr > /dev/null
+        [ "$?" == "0" ] && MAC_FROM_CMDLINE=`echo "${MACADDR}" | sed 's/mac_addr=//g'`
+done
+
+if [ -f /etc/sysconfig/mac_addr ];  then
+        . /etc/sysconfig/mac_addr
+fi
+
+case "$1" in
+  start)
+ 	echo "Starting network..."
+	if [ "$NET_USE_DHCP" == "Y" ]; then
+		ifconfig ${NET_DEVICE} hw ether ${MAC_FROM_CMDLINE} up
+		udhcpc -i ${NET_DEVICE} &
+	else
+		echo "search ${NET_SUFFIX}" > /etc/resolv.conf
+		echo "nameserver ${NET_DNS_LIST}" >> /etc/resolv.conf
+		ifconfig ${NET_DEVICE} ${NET_IP_ADDRESS} netmask ${NET_MASK} hw ether ${MAC_FROM_CMDLINE} up >/dev/null 2>&1
+		route add default gw ${NET_GATEWAY} dev ${NET_DEVICE} >/dev/null 2>&1
+		echo "${NET_IP_ADDRESS}" > /tmp/my_ip
+	fi
+	if [ -f /usr/sbin/telnetd ]; then
+		telnetd
+	fi
+	if [ -f /usr/sbin/inetd ]; then
+		inetd
+	fi
+	;;
+  stop)
+	echo -n "Stopping network..."
+	/sbin/ifdown -a
+	kill -9 `pidof telnetd`
+	;;
+  restart|reload)
+	"$0" stop
+	"$0" start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S49ntp SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S49ntp
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S49ntp	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S49ntp	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,6 @@
+#! /bin/sh
+#
+# Empty, used after network is up
+#
+
+exit 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S50sshd SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S50sshd
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S50sshd	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S50sshd	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,57 @@
+#!/bin/sh
+#
+# sshd        Starts sshd.
+#
+
+# Make sure the ssh-keygen progam exists
+[ -f /usr/bin/ssh-keygen ] || exit 0
+
+mount /dev/mmcblk2p3 /mnt
+if [ -d /mnt/sysconfig/ssh ]; then
+	cp -r /mnt/sysconfig/ss* /etc/.
+else
+	# Create any missing keys
+	/usr/bin/ssh-keygen -A
+	if [ ! -d /mnt/sysconfig ]; then
+		mkdir /mnt/sysconfig
+	fi
+	cp -r /etc/ss* /mnt/sysconfig/.
+fi
+umount /mnt
+
+umask 077
+
+start() {
+	echo -n "Starting sshd: "
+	/usr/sbin/sshd
+	touch /var/lock/sshd
+	echo "OK"
+}
+stop() {
+	echo -n "Stopping sshd: "
+	killall sshd
+	rm -f /var/lock/sshd
+	echo "OK"
+}
+restart() {
+	stop
+	start
+}
+
+case "$1" in
+  start)
+	start
+	;;
+  stop)
+	stop
+	;;
+  restart|reload)
+	restart
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S50telnet SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S50telnet
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S50telnet	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S50telnet	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,36 @@
+#!/bin/sh
+#
+# Start telnet....
+#
+
+start() {
+      printf "Starting telnetd: "
+      start-stop-daemon -S -q -m -b -p /var/run/telnetd.pid \
+			-x /usr/sbin/telnetd -- -F
+      [ $? = 0 ] && echo "OK" || echo "FAIL"
+}
+
+stop() {
+	printf "Stopping telnetd: "
+	start-stop-daemon -K -q -p /var/run/telnetd.pid \
+			  -x /usr/sbin/telnetd
+	[ $? = 0 ] && echo "OK" || echo "FAIL"
+}
+
+case "$1" in
+    start)
+	start
+	;;
+    stop)
+	stop
+	;;
+    restart|reload)
+	stop
+	start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S97mysqld SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S97mysqld
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/init.d/S97mysqld	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/init.d/S97mysqld	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,38 @@
+#!/bin/sh
+[ ! -f /usr/bin/mysql ] && exit 0
+
+case "$1" in
+	start)
+		if [ ! -d /tmp/mysql ] ; then
+			echo "Creating MySQL system tables..."
+			mysql_install_db --user=mysql --ldata=/tmp/mysql
+		fi
+
+		# We don't use start-stop-daemon because mysqld has
+		# its own wrapper script.
+		echo -n "Starting mysql..."
+		/usr/bin/mysqld_safe --pid-file=/var/run/mysqld.pid  --datadir=/tmp/mysql &
+		usleep 900000
+mysql << _EOF_
+GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' IDENTIFIED BY 'novasis';
+quit
+_EOF_
+
+
+		echo "done."
+		;;
+	stop)
+		echo -n "Stopping mysql..."
+		if test -f /var/run/mysqld.pid ; then
+			kill `cat /var/run/mysqld.pid`
+		fi
+		echo "done."
+		;;
+	restart)
+		$0 stop
+		$0 start
+		;;
+	*)
+		echo "Usage: /etc/init.d/mysqld {start|stop|restart}"
+		;;
+esac
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/inittab SDLBase/board/novasis/NOVAsomS/Init/etc/inittab
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/inittab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/inittab	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,35 @@
+# /etc/inittab
+#
+# Copyright (C) 2001 Erik Andersen <andersen@codepoet.org>
+#
+# Note: BusyBox init doesn't support runlevels.  The runlevels field is
+# completely ignored by BusyBox init. If you want runlevels, use
+# sysvinit.
+#
+# Format for each entry: <id>:<runlevels>:<action>:<process>
+#
+# id        == tty to run on, or empty for /dev/console
+# runlevels == ignored
+# action    == one of sysinit, respawn, askfirst, wait, and once
+# process   == program to run
+
+# Startup the system
+::sysinit:/bin/mount -t proc proc /proc
+::sysinit:/bin/mount -o remount,rw /
+::sysinit:/bin/mkdir -p /dev/pts
+::sysinit:/bin/mkdir -p /dev/shm
+::sysinit:/bin/mount -a
+::sysinit:/bin/hostname -F /etc/hostname
+# now run any rc scripts
+::sysinit:/etc/init.d/rcS
+
+# Put a getty on the serial port
+ttymxc0::respawn:/sbin/getty -L -l /bin/autologin.sh -n ttymxc0 115200 vt100 # GENERIC_SERIAL
+
+# Stuff to do for the 3-finger salute
+#::ctrlaltdel:/sbin/reboot
+
+# Stuff to do before rebooting
+::shutdown:/etc/init.d/rcK
+::shutdown:/sbin/swapoff -a
+::shutdown:/bin/umount -a -r
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/issue SDLBase/board/novasis/NOVAsomS/Init/etc/issue
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/issue	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/issue	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1 @@
+Welcome
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/mtab SDLBase/board/novasis/NOVAsomS/Init/etc/mtab
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/mtab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/mtab	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,30 @@
+sysfs /sys sysfs rw,nosuid,nodev,noexec,relatime 0 0
+proc /proc proc rw,nosuid,nodev,noexec,relatime 0 0
+udev /dev devtmpfs rw,nosuid,relatime,size=4971252k,nr_inodes=1242813,mode=755 0 0
+devpts /dev/pts devpts rw,nosuid,noexec,relatime,gid=5,mode=620,ptmxmode=000 0 0
+tmpfs /run tmpfs rw,nosuid,noexec,relatime,size=998148k,mode=755 0 0
+/dev/sda1 / ext4 rw,relatime,errors=remount-ro,data=ordered 0 0
+securityfs /sys/kernel/security securityfs rw,nosuid,nodev,noexec,relatime 0 0
+tmpfs /dev/shm tmpfs rw,nosuid,nodev 0 0
+tmpfs /run/lock tmpfs rw,nosuid,nodev,noexec,relatime,size=5120k 0 0
+tmpfs /sys/fs/cgroup tmpfs ro,nosuid,nodev,noexec,mode=755 0 0
+cgroup /sys/fs/cgroup/systemd cgroup rw,nosuid,nodev,noexec,relatime,xattr,release_agent=/lib/systemd/systemd-cgroups-agent,name=systemd 0 0
+pstore /sys/fs/pstore pstore rw,nosuid,nodev,noexec,relatime 0 0
+cgroup /sys/fs/cgroup/net_cls,net_prio cgroup rw,nosuid,nodev,noexec,relatime,net_cls,net_prio 0 0
+cgroup /sys/fs/cgroup/pids cgroup rw,nosuid,nodev,noexec,relatime,pids 0 0
+cgroup /sys/fs/cgroup/cpu,cpuacct cgroup rw,nosuid,nodev,noexec,relatime,cpu,cpuacct 0 0
+cgroup /sys/fs/cgroup/devices cgroup rw,nosuid,nodev,noexec,relatime,devices 0 0
+cgroup /sys/fs/cgroup/hugetlb cgroup rw,nosuid,nodev,noexec,relatime,hugetlb 0 0
+cgroup /sys/fs/cgroup/perf_event cgroup rw,nosuid,nodev,noexec,relatime,perf_event 0 0
+cgroup /sys/fs/cgroup/freezer cgroup rw,nosuid,nodev,noexec,relatime,freezer 0 0
+cgroup /sys/fs/cgroup/cpuset cgroup rw,nosuid,nodev,noexec,relatime,cpuset 0 0
+cgroup /sys/fs/cgroup/memory cgroup rw,nosuid,nodev,noexec,relatime,memory 0 0
+cgroup /sys/fs/cgroup/blkio cgroup rw,nosuid,nodev,noexec,relatime,blkio 0 0
+systemd-1 /proc/sys/fs/binfmt_misc autofs rw,relatime,fd=26,pgrp=1,timeout=0,minproto=5,maxproto=5,direct 0 0
+debugfs /sys/kernel/debug debugfs rw,relatime 0 0
+hugetlbfs /dev/hugepages hugetlbfs rw,relatime 0 0
+mqueue /dev/mqueue mqueue rw,relatime 0 0
+fusectl /sys/fs/fuse/connections fusectl rw,relatime 0 0
+UShared /media/sf_UShared vboxsf rw,nodev,relatime 0 0
+tmpfs /run/user/1000 tmpfs rw,nosuid,nodev,relatime,size=998148k,mode=700,uid=1000,gid=1000 0 0
+gvfsd-fuse /run/user/1000/gvfs fuse.gvfsd-fuse rw,nosuid,nodev,relatime,user_id=1000,group_id=1000 0 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/network/if-pre-up.d/wait_iface SDLBase/board/novasis/NOVAsomS/Init/etc/network/if-pre-up.d/wait_iface
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/network/if-pre-up.d/wait_iface	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/network/if-pre-up.d/wait_iface	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,21 @@
+#!/bin/sh
+
+# In case we have a slow-to-appear interface (e.g. eth-over-USB),
+# and we need to configure it, wait until it appears, but not too
+# long either. IF_WAIT_DELAY is in seconds.
+
+if [ "${IF_WAIT_DELAY}" -a ! -e "/sys/class/net/${IFACE}" ]; then
+    printf "Waiting for interface %s to appear" "${IFACE}"
+    while [ ${IF_WAIT_DELAY} -gt 0 ]; do
+        if [ -e "/sys/class/net/${IFACE}" ]; then
+            printf "\n"
+            exit 0
+        fi
+        sleep 1
+        printf "."
+        : $((IF_WAIT_DELAY -= 1))
+    done
+    printf " timeout!\n"
+    exit 1
+fi
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/network/interfaces SDLBase/board/novasis/NOVAsomS/Init/etc/network/interfaces
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/network/interfaces	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/network/interfaces	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+# interface file auto-generated by buildroot
+
+auto lo
+iface lo inet loopback
+
+auto eth0
+iface eth0 inet dhcp
+	pre-up /etc/network/nfs_check
+	wait-delay 15
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/network/nfs_check SDLBase/board/novasis/NOVAsomS/Init/etc/network/nfs_check
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/network/nfs_check	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/network/nfs_check	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,20 @@
+#!/bin/sh
+
+# This allows NFS booting to work while also being able to configure
+# the network interface via DHCP when not NFS booting.  Otherwise, a
+# NFS booted system will likely hang during DHCP configuration.
+
+# Attempting to configure the network interface used for NFS will
+# initially bring that network down.  Since the root filesystem is
+# accessed over this network, the system hangs.
+
+# This script is run by ifup and will attempt to detect if a NFS root
+# mount uses the interface to be configured (IFACE), and if so does
+# not configure it.  This should allow the same build to be disk/flash
+# booted or NFS booted.
+
+nfsip=`sed -n '/^[^ ]*:.* \/ nfs.*[ ,]addr=\([0-9.]\+\).*/s//\1/p' /proc/mounts`
+if [ -n "$nfsip" ] && ip route get to "$nfsip" | grep -q "dev $IFACE"; then
+	echo Skipping $IFACE, used for NFS from $nfsip
+	exit 1
+fi
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/nsswitch.conf SDLBase/board/novasis/NOVAsomS/Init/etc/nsswitch.conf
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/nsswitch.conf	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/nsswitch.conf	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,13 @@
+# /etc/nsswitch.conf
+
+passwd:         files
+group:          files
+shadow:         files
+
+hosts:          files dns
+networks:       files dns
+
+protocols:      files
+services:       files
+ethers:         files
+rpc:            files
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/os-release SDLBase/board/novasis/NOVAsomS/Init/etc/os-release
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/os-release	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/os-release	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,5 @@
+NAME=Buildroot
+VERSION=2016.05
+ID=buildroot
+VERSION_ID=2016.05
+PRETTY_NAME="Buildroot 2016.05"
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/passwd SDLBase/board/novasis/NOVAsomS/Init/etc/passwd
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/passwd	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/passwd	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+root:x:0:0:root:/root:/bin/sh
+daemon:x:1:1:daemon:/usr/sbin:/bin/false
+bin:x:2:2:bin:/bin:/bin/false
+sys:x:3:3:sys:/dev:/bin/false
+sync:x:4:100:sync:/bin:/bin/sync
+mail:x:8:8:mail:/var/spool/mail:/bin/false
+www-data:x:33:33:www-data:/var/www:/bin/false
+operator:x:37:37:Operator:/var:/bin/false
+nobody:x:99:99:nobody:/home:/bin/false
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/profile SDLBase/board/novasis/NOVAsomS/Init/etc/profile
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/profile	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/profile	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,20 @@
+export PATH=/bin:/sbin:/usr/bin:/usr/sbin
+
+if [ "$PS1" ]; then
+	if [ "`id -u`" -eq 0 ]; then
+		export PS1='# '
+	else
+		export PS1='$ '
+	fi
+fi
+
+export PAGER='/bin/more '
+export EDITOR='/bin/vi'
+
+# Source configuration files from /etc/profile.d
+for i in /etc/profile.d/*.sh ; do
+	if [ -r "$i" ]; then
+		. $i
+	fi
+	unset i
+done
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/profile.d/umask.sh SDLBase/board/novasis/NOVAsomS/Init/etc/profile.d/umask.sh
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/profile.d/umask.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/profile.d/umask.sh	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1 @@
+umask 022
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/protocols SDLBase/board/novasis/NOVAsomS/Init/etc/protocols
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/protocols	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/protocols	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,61 @@
+# Internet (IP) protocols
+#
+# Updated from http://www.iana.org/assignments/protocol-numbers and other
+# sources.
+
+ip	0	IP		# internet protocol, pseudo protocol number
+hopopt	0	HOPOPT		# IPv6 Hop-by-Hop Option [RFC1883]
+icmp	1	ICMP		# internet control message protocol
+igmp	2	IGMP		# Internet Group Management
+ggp	3	GGP		# gateway-gateway protocol
+ipencap	4	IP-ENCAP	# IP encapsulated in IP (officially ``IP'')
+st	5	ST		# ST datagram mode
+tcp	6	TCP		# transmission control protocol
+egp	8	EGP		# exterior gateway protocol
+igp	9	IGP		# any private interior gateway (Cisco)
+pup	12	PUP		# PARC universal packet protocol
+udp	17	UDP		# user datagram protocol
+hmp	20	HMP		# host monitoring protocol
+xns-idp	22	XNS-IDP		# Xerox NS IDP
+rdp	27	RDP		# "reliable datagram" protocol
+iso-tp4	29	ISO-TP4		# ISO Transport Protocol class 4 [RFC905]
+dccp	33	DCCP		# Datagram Congestion Control Prot. [RFC4340]
+xtp	36	XTP		# Xpress Transfer Protocol
+ddp	37	DDP		# Datagram Delivery Protocol
+idpr-cmtp 38	IDPR-CMTP	# IDPR Control Message Transport
+ipv6	41	IPv6		# Internet Protocol, version 6
+ipv6-route 43	IPv6-Route	# Routing Header for IPv6
+ipv6-frag 44	IPv6-Frag	# Fragment Header for IPv6
+idrp	45	IDRP		# Inter-Domain Routing Protocol
+rsvp	46	RSVP		# Reservation Protocol
+gre	47	GRE		# General Routing Encapsulation
+esp	50	IPSEC-ESP	# Encap Security Payload [RFC2406]
+ah	51	IPSEC-AH	# Authentication Header [RFC2402]
+skip	57	SKIP		# SKIP
+ipv6-icmp 58	IPv6-ICMP	# ICMP for IPv6
+ipv6-nonxt 59	IPv6-NoNxt	# No Next Header for IPv6
+ipv6-opts 60	IPv6-Opts	# Destination Options for IPv6
+rspf	73	RSPF CPHB	# Radio Shortest Path First (officially CPHB)
+vmtp	81	VMTP		# Versatile Message Transport
+eigrp	88	EIGRP		# Enhanced Interior Routing Protocol (Cisco)
+ospf	89	OSPFIGP		# Open Shortest Path First IGP
+ax.25	93	AX.25		# AX.25 frames
+ipip	94	IPIP		# IP-within-IP Encapsulation Protocol
+etherip	97	ETHERIP		# Ethernet-within-IP Encapsulation [RFC3378]
+encap	98	ENCAP		# Yet Another IP encapsulation [RFC1241]
+#	99			# any private encryption scheme
+pim	103	PIM		# Protocol Independent Multicast
+ipcomp	108	IPCOMP		# IP Payload Compression Protocol
+vrrp	112	VRRP		# Virtual Router Redundancy Protocol [RFC5798]
+l2tp	115	L2TP		# Layer Two Tunneling Protocol [RFC2661]
+isis	124	ISIS		# IS-IS over IPv4
+sctp	132	SCTP		# Stream Control Transmission Protocol
+fc	133	FC		# Fibre Channel
+mobility-header 135 Mobility-Header # Mobility Support for IPv6 [RFC3775]
+udplite	136	UDPLite		# UDP-Lite [RFC3828]
+mpls-in-ip 137	MPLS-in-IP	# MPLS-in-IP [RFC4023]
+manet	138			# MANET Protocols [RFC5498]
+hip	139	HIP		# Host Identity Protocol
+shim6	140	Shim6		# Shim6 Protocol [RFC5533]
+wesp	141	WESP		# Wrapped Encapsulating Security Payload
+rohc	142	ROHC		# Robust Header Compression
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/services SDLBase/board/novasis/NOVAsomS/Init/etc/services
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/services	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/services	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,302 @@
+# /etc/services:
+# $Id: services,v 1.1 2004/10/09 02:49:18 andersen Exp $
+#
+# Network services, Internet style
+#
+# Note that it is presently the policy of IANA to assign a single well-known
+# port number for both TCP and UDP; hence, most entries here have two entries
+# even if the protocol doesn't support UDP operations.
+# Updated from RFC 1700, ``Assigned Numbers'' (October 1994).  Not all ports
+# are included, only the more common ones.
+
+tcpmux		1/tcp				# TCP port service multiplexer
+echo		7/tcp
+echo		7/udp
+discard		9/tcp		sink null
+discard		9/udp		sink null
+systat		11/tcp		users
+daytime		13/tcp
+daytime		13/udp
+netstat		15/tcp
+qotd		17/tcp		quote
+msp		18/tcp				# message send protocol
+msp		18/udp				# message send protocol
+chargen		19/tcp		ttytst source
+chargen		19/udp		ttytst source
+ftp-data	20/tcp
+ftp		21/tcp
+fsp		21/udp		fspd
+ssh		22/tcp				# SSH Remote Login Protocol
+ssh		22/udp				# SSH Remote Login Protocol
+telnet		23/tcp
+# 24 - private
+smtp		25/tcp		mail
+# 26 - unassigned
+time		37/tcp		timserver
+time		37/udp		timserver
+rlp		39/udp		resource	# resource location
+nameserver	42/tcp		name		# IEN 116
+whois		43/tcp		nicname
+re-mail-ck	50/tcp				# Remote Mail Checking Protocol
+re-mail-ck	50/udp				# Remote Mail Checking Protocol
+domain		53/tcp		nameserver	# name-domain server
+domain		53/udp		nameserver
+mtp		57/tcp				# deprecated
+bootps		67/tcp				# BOOTP server
+bootps		67/udp
+bootpc		68/tcp				# BOOTP client
+bootpc		68/udp
+tftp		69/udp
+gopher		70/tcp				# Internet Gopher
+gopher		70/udp
+rje		77/tcp		netrjs
+finger		79/tcp
+www		80/tcp		http		# WorldWideWeb HTTP
+www		80/udp				# HyperText Transfer Protocol
+link		87/tcp		ttylink
+kerberos	88/tcp		kerberos5 krb5	# Kerberos v5
+kerberos	88/udp		kerberos5 krb5	# Kerberos v5
+supdup		95/tcp
+# 100 - reserved
+hostnames	101/tcp		hostname	# usually from sri-nic
+iso-tsap	102/tcp		tsap		# part of ISODE.
+csnet-ns	105/tcp		cso-ns		# also used by CSO name server
+csnet-ns	105/udp		cso-ns
+# unfortunately the poppassd (Eudora) uses a port which has already
+# been assigned to a different service. We list the poppassd as an
+# alias here. This should work for programs asking for this service.
+# (due to a bug in inetd the 3com-tsmux line is disabled)
+#3com-tsmux	106/tcp		poppassd
+#3com-tsmux	106/udp		poppassd
+rtelnet		107/tcp				# Remote Telnet
+rtelnet		107/udp
+pop-2		109/tcp		postoffice	# POP version 2
+pop-2		109/udp
+pop-3		110/tcp				# POP version 3
+pop-3		110/udp
+sunrpc		111/tcp		portmapper	# RPC 4.0 portmapper TCP
+sunrpc		111/udp		portmapper	# RPC 4.0 portmapper UDP
+auth		113/tcp		authentication tap ident
+sftp		115/tcp
+uucp-path	117/tcp
+nntp		119/tcp		readnews untp	# USENET News Transfer Protocol
+ntp		123/tcp
+ntp		123/udp				# Network Time Protocol
+netbios-ns	137/tcp				# NETBIOS Name Service
+netbios-ns	137/udp
+netbios-dgm	138/tcp				# NETBIOS Datagram Service
+netbios-dgm	138/udp
+netbios-ssn	139/tcp				# NETBIOS session service
+netbios-ssn	139/udp
+imap2		143/tcp				# Interim Mail Access Proto v2
+imap2		143/udp
+snmp		161/udp				# Simple Net Mgmt Proto
+snmp-trap	162/udp		snmptrap	# Traps for SNMP
+cmip-man	163/tcp				# ISO mgmt over IP (CMOT)
+cmip-man	163/udp
+cmip-agent	164/tcp
+cmip-agent	164/udp
+xdmcp		177/tcp				# X Display Mgr. Control Proto
+xdmcp		177/udp
+nextstep	178/tcp		NeXTStep NextStep	# NeXTStep window
+nextstep	178/udp		NeXTStep NextStep	# server
+bgp		179/tcp				# Border Gateway Proto.
+bgp		179/udp
+prospero	191/tcp				# Cliff Neuman's Prospero
+prospero	191/udp
+irc		194/tcp				# Internet Relay Chat
+irc		194/udp
+smux		199/tcp				# SNMP Unix Multiplexer
+smux		199/udp
+at-rtmp		201/tcp				# AppleTalk routing
+at-rtmp		201/udp
+at-nbp		202/tcp				# AppleTalk name binding
+at-nbp		202/udp
+at-echo		204/tcp				# AppleTalk echo
+at-echo		204/udp
+at-zis		206/tcp				# AppleTalk zone information
+at-zis		206/udp
+qmtp		209/tcp				# The Quick Mail Transfer Protocol
+qmtp		209/udp				# The Quick Mail Transfer Protocol
+z3950		210/tcp		wais		# NISO Z39.50 database
+z3950		210/udp		wais
+ipx		213/tcp				# IPX
+ipx		213/udp
+imap3		220/tcp				# Interactive Mail Access
+imap3		220/udp				# Protocol v3
+ulistserv	372/tcp				# UNIX Listserv
+ulistserv	372/udp
+https		443/tcp				# MCom
+https		443/udp				# MCom
+snpp		444/tcp				# Simple Network Paging Protocol
+snpp		444/udp				# Simple Network Paging Protocol
+saft		487/tcp				# Simple Asynchronous File Transfer
+saft		487/udp				# Simple Asynchronous File Transfer
+npmp-local	610/tcp		dqs313_qmaster	# npmp-local / DQS
+npmp-local	610/udp		dqs313_qmaster	# npmp-local / DQS
+npmp-gui	611/tcp		dqs313_execd	# npmp-gui / DQS
+npmp-gui	611/udp		dqs313_execd	# npmp-gui / DQS
+hmmp-ind	612/tcp		dqs313_intercell# HMMP Indication / DQS
+hmmp-ind	612/udp		dqs313_intercell# HMMP Indication / DQS
+#
+# UNIX specific services
+#
+exec		512/tcp
+biff		512/udp		comsat
+login		513/tcp
+who		513/udp		whod
+shell		514/tcp		cmd		# no passwords used
+syslog		514/udp
+printer		515/tcp		spooler		# line printer spooler
+talk		517/udp
+ntalk		518/udp
+route		520/udp		router routed	# RIP
+timed		525/udp		timeserver
+tempo		526/tcp		newdate
+courier		530/tcp		rpc
+conference	531/tcp		chat
+netnews		532/tcp		readnews
+netwall		533/udp				# -for emergency broadcasts
+uucp		540/tcp		uucpd		# uucp daemon
+afpovertcp	548/tcp				# AFP over TCP
+afpovertcp	548/udp				# AFP over TCP
+remotefs	556/tcp		rfs_server rfs	# Brunhoff remote filesystem
+klogin		543/tcp				# Kerberized `rlogin' (v5)
+kshell		544/tcp		krcmd		# Kerberized `rsh' (v5)
+kerberos-adm	749/tcp				# Kerberos `kadmin' (v5)
+#
+webster		765/tcp				# Network dictionary
+webster		765/udp
+#
+# From ``Assigned Numbers'':
+#
+#> The Registered Ports are not controlled by the IANA and on most systems
+#> can be used by ordinary user processes or programs executed by ordinary
+#> users.
+#
+#> Ports are used in the TCP [45,106] to name the ends of logical
+#> connections which carry long term conversations.  For the purpose of
+#> providing services to unknown callers, a service contact port is
+#> defined.  This list specifies the port used by the server process as its
+#> contact port.  While the IANA can not control uses of these ports it
+#> does register or list uses of these ports as a convienence to the
+#> community.
+#
+nfsdstatus	1110/tcp
+nfsd-keepalive	1110/udp
+
+ingreslock	1524/tcp
+ingreslock	1524/udp
+prospero-np	1525/tcp			# Prospero non-privileged
+prospero-np	1525/udp
+datametrics	1645/tcp	old-radius	# datametrics / old radius entry
+datametrics	1645/udp	old-radius	# datametrics / old radius entry
+sa-msg-port	1646/tcp	old-radacct	# sa-msg-port / old radacct entry
+sa-msg-port	1646/udp	old-radacct	# sa-msg-port / old radacct entry
+radius		1812/tcp			# Radius
+radius		1812/udp			# Radius
+radacct		1813/tcp			# Radius Accounting
+radacct		1813/udp			# Radius Accounting
+nfsd		2049/tcp	nfs
+nfsd		2049/udp	nfs
+cvspserver	2401/tcp			# CVS client/server operations
+cvspserver	2401/udp			# CVS client/server operations
+mysql		3306/tcp			# MySQL
+mysql		3306/udp			# MySQL
+rfe		5002/tcp			# Radio Free Ethernet
+rfe		5002/udp			# Actually uses UDP only
+cfengine	5308/tcp			# CFengine
+cfengine	5308/udp			# CFengine
+bbs		7000/tcp			# BBS service
+#
+#
+# Kerberos (Project Athena/MIT) services
+# Note that these are for Kerberos v4, and are unofficial.  Sites running
+# v4 should uncomment these and comment out the v5 entries above.
+#
+kerberos4	750/udp		kerberos-iv kdc	# Kerberos (server) udp
+kerberos4	750/tcp		kerberos-iv kdc	# Kerberos (server) tcp
+kerberos_master	751/udp				# Kerberos authentication
+kerberos_master	751/tcp				# Kerberos authentication
+passwd_server	752/udp				# Kerberos passwd server
+krb_prop	754/tcp				# Kerberos slave propagation
+krbupdate	760/tcp		kreg		# Kerberos registration
+kpasswd		761/tcp		kpwd		# Kerberos "passwd"
+kpop		1109/tcp			# Pop with Kerberos
+knetd		2053/tcp			# Kerberos de-multiplexor
+zephyr-srv	2102/udp			# Zephyr server
+zephyr-clt	2103/udp			# Zephyr serv-hm connection
+zephyr-hm	2104/udp			# Zephyr hostmanager
+eklogin		2105/tcp			# Kerberos encrypted rlogin
+#
+# Unofficial but necessary (for NetBSD) services
+#
+supfilesrv	871/tcp				# SUP server
+supfiledbg	1127/tcp			# SUP debugging
+#
+# Datagram Delivery Protocol services
+#
+rtmp		1/ddp				# Routing Table Maintenance Protocol
+nbp		2/ddp				# Name Binding Protocol
+echo		4/ddp				# AppleTalk Echo Protocol
+zip		6/ddp				# Zone Information Protocol
+#
+# Services added for the Debian GNU/Linux distribution
+poppassd	106/tcp				# Eudora
+poppassd	106/udp				# Eudora
+mailq		174/tcp				# Mailer transport queue for Zmailer
+mailq		174/tcp				# Mailer transport queue for Zmailer
+omirr		808/tcp		omirrd		# online mirror
+omirr		808/udp		omirrd		# online mirror
+rmtcfg		1236/tcp			# Gracilis Packeten remote config server
+xtel		1313/tcp			# french minitel
+coda_opcons	1355/udp			# Coda opcons            (Coda fs)
+coda_venus	1363/udp			# Coda venus             (Coda fs)
+coda_auth	1357/udp			# Coda auth              (Coda fs)
+coda_udpsrv	1359/udp			# Coda udpsrv            (Coda fs)
+coda_filesrv	1361/udp			# Coda filesrv           (Coda fs)
+codacon		1423/tcp	venus.cmu	# Coda Console           (Coda fs)
+coda_aux1	1431/tcp			# coda auxiliary service (Coda fs)
+coda_aux1	1431/udp			# coda auxiliary service (Coda fs)
+coda_aux2	1433/tcp			# coda auxiliary service (Coda fs)
+coda_aux2	1433/udp			# coda auxiliary service (Coda fs)
+coda_aux3	1435/tcp			# coda auxiliary service (Coda fs)
+coda_aux3	1435/udp			# coda auxiliary service (Coda fs)
+cfinger		2003/tcp			# GNU Finger
+afbackup	2988/tcp			# Afbackup system
+afbackup	2988/udp			# Afbackup system
+icp		3130/tcp			# Internet Cache Protocol (Squid)
+icp		3130/udp			# Internet Cache Protocol (Squid)
+postgres	5432/tcp			# POSTGRES
+postgres	5432/udp			# POSTGRES
+fax		4557/tcp			# FAX transmission service        (old)
+hylafax		4559/tcp			# HylaFAX client-server protocol  (new)
+noclog		5354/tcp			# noclogd with TCP (nocol)
+noclog		5354/udp			# noclogd with UDP (nocol)
+hostmon		5355/tcp			# hostmon uses TCP (nocol)
+hostmon		5355/udp			# hostmon uses TCP (nocol)
+ircd		6667/tcp			# Internet Relay Chat
+ircd		6667/udp			# Internet Relay Chat
+webcache	8080/tcp			# WWW caching service
+webcache	8080/udp			# WWW caching service
+tproxy		8081/tcp			# Transparent Proxy
+tproxy		8081/udp			# Transparent Proxy
+mandelspawn	9359/udp	mandelbrot	# network mandelbrot
+amanda		10080/udp			# amanda backup services
+amandaidx	10082/tcp			# amanda backup services
+amidxtape	10083/tcp			# amanda backup services
+isdnlog		20011/tcp			# isdn logging system
+isdnlog		20011/udp			# isdn logging system
+vboxd		20012/tcp			# voice box system
+vboxd		20012/udp			# voice box system
+binkp           24554/tcp			# Binkley
+binkp           24554/udp			# Binkley
+asp		27374/tcp			# Address Search Protocol
+asp		27374/udp			# Address Search Protocol
+tfido           60177/tcp			# Ifmail
+tfido           60177/udp			# Ifmail
+fido            60179/tcp			# Ifmail
+fido            60179/udp			# Ifmail
+
+# Local services
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/shadow SDLBase/board/novasis/NOVAsomS/Init/etc/shadow
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/shadow	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/shadow	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+root::10933:0:99999:7:::
+daemon:*:10933:0:99999:7:::
+bin:*:10933:0:99999:7:::
+sys:*:10933:0:99999:7:::
+sync:*:10933:0:99999:7:::
+mail:*:10933:0:99999:7:::
+www-data:*:10933:0:99999:7:::
+operator:*:10933:0:99999:7:::
+nobody:*:10933:0:99999:7:::
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/sysconfig/network SDLBase/board/novasis/NOVAsomS/Init/etc/sysconfig/network
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/sysconfig/network	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/sysconfig/network	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,19 @@
+# Wired ethernet
+ETH0_ENABLED=Y
+NET_DEVICE=eth0
+NET_SUFFIX=novasis.it
+# Maybe N or Y
+# NET_USE_DHCP=N
+NET_USE_DHCP=Y
+#The following will be ignored if NET_USE_DHCP=N
+NET_IP_ADDRESS=192.168.10.51
+NET_BROADCAST=192.168.10.51
+NET_MASK=255.255.255.0
+NET_HOSTNAME=Novasom6_Ethernet
+NET_NETWORK=192.168.10.0
+NET_GATEWAY=192.168.10.1
+NET_DNS_LIST=192.168.10.200
+NET_DNS_DOMAIN=-
+NET_ALIAS=192.168.10.248
+NET_HTTPD_PORT=8080
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/sysconfig/system_vars SDLBase/board/novasis/NOVAsomS/Init/etc/sysconfig/system_vars
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/sysconfig/system_vars	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/sysconfig/system_vars	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1 @@
+REFERENCE_SERVER=192.168.10.132
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/sysconfig/wireless SDLBase/board/novasis/NOVAsomS/Init/etc/sysconfig/wireless
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/etc/sysconfig/wireless	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/etc/sysconfig/wireless	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,16 @@
+#Wireless
+WLAN_ENABLED=N
+WLAN_USE_DHCP=N
+WLAN_DEVICE=wlan0
+WLAN_SUFFIX=novasis.it
+#The following will be ignored if WLAN_USE_DHCP=OFF
+WLAN_IP_ADDRESS=192.168.10.199
+WLAN_BROADCAST=192.168.10.199
+WLAN_MASK=255.255.255.0
+WLAN_HOSTNAME=Novasom6_Wlan
+WLAN_NETWORK=192.168.10.0
+WLAN_GATEWAY=192.168.10.1
+WLAN_DNS_LIST=192.168.10.200
+WLAN_DNS_DOMAIN=-
+WLAN_MAC=1E:72:49:ED:F7:54
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_not_present SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_not_present
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_not_present	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_not_present	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,38 @@
+pcm.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+ctl.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+pcm.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+ctl.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+pcm.copy {
+        type plug
+        slave {
+                pcm hw
+        }
+        route_policy copy
+}
+pcm.!default {
+        type hw
+        card 0
+        channels 2
+}
+ctl.!default {
+        type hw
+        card 0
+        channels 2
+}
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_codec SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_codec
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_codec	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_codec	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,48 @@
+pcm.sgtl5000audio {
+        type hw   
+        card 0   
+        channels 2
+}
+ctl.sgtl5000audio {
+        type hw
+        card 0
+        channels 2
+}
+pcm.imxspdif {
+        type hw
+        card 1
+        channels 2
+}
+ctl.imxspdif {
+        type hw
+        card 1
+        channels 2
+}
+pcm.imxhdmisoc {
+        type hw
+        card 2
+        channels 2
+}
+ctl.imxhdmisoc {
+        type hw
+        card 2
+        channels 2
+}
+pcm.copy {
+        type plug
+        slave {
+                pcm hw
+        }
+        route_policy copy
+}
+pcm.!default {
+        type hw
+        card 0
+        channels 2
+}
+ctl.!default {
+        type hw
+        card 0
+        channels 2
+}
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_hdmi SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_hdmi
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_hdmi	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_hdmi	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,48 @@
+pcm.sgtl5000audio {
+        type hw   
+        card 0   
+        channels 2
+}
+ctl.sgtl5000audio {
+        type hw
+        card 0
+        channels 2
+}
+pcm.imxspdif {
+        type hw
+        card 1
+        channels 2
+}
+ctl.imxspdif {
+        type hw
+        card 1
+        channels 2
+}
+pcm.imxhdmisoc {
+        type hw
+        card 2
+        channels 2
+}
+ctl.imxhdmisoc {
+        type hw
+        card 2
+        channels 2
+}
+pcm.copy {
+        type plug
+        slave {
+                pcm hw
+        }
+        route_policy copy
+}
+pcm.!default {
+        type hw
+        card 2
+        channels 2
+}
+ctl.!default {
+        type hw
+        card 2
+        channels 2
+}
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_spdif SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_spdif
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_spdif	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/root/alsa_audio/codec_present_default_spdif	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,48 @@
+pcm.sgtl5000audio {
+        type hw   
+        card 0   
+        channels 2
+}
+ctl.sgtl5000audio {
+        type hw
+        card 0
+        channels 2
+}
+pcm.imxspdif {
+        type hw
+        card 1
+        channels 2
+}
+ctl.imxspdif {
+        type hw
+        card 1
+        channels 2
+}
+pcm.imxhdmisoc {
+        type hw
+        card 2
+        channels 2
+}
+ctl.imxhdmisoc {
+        type hw
+        card 2
+        channels 2
+}
+pcm.copy {
+        type plug
+        slave {
+                pcm hw
+        }
+        route_policy copy
+}
+pcm.!default {
+        type hw
+        card 1
+        channels 2
+}
+ctl.!default {
+        type hw
+        card 1
+        channels 2
+}
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/root/.asoundrc SDLBase/board/novasis/NOVAsomS/Init/root/.asoundrc
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/root/.asoundrc	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/root/.asoundrc	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,38 @@
+pcm.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+ctl.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+pcm.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+ctl.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+pcm.copy {
+        type plug
+        slave {
+                pcm hw
+        }
+        route_policy copy
+}
+pcm.!default {
+        type hw
+        card 1
+        channels 2
+}
+ctl.!default {
+        type hw
+        card 1
+        channels 2
+}
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/root/.profile SDLBase/board/novasis/NOVAsomS/Init/root/.profile
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/root/.profile	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/root/.profile	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,10 @@
+#
+# .profile, started at boot from root user
+# 
+if [ -d /tmp/application_storage ]; then
+        if [ -f /tmp/application_storage/AutoRun.sh ]; then
+                chmod 777 /tmp/application_storage/AutoRun.sh
+                /tmp/application_storage/AutoRun.sh &
+        fi
+fi
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/Init/usr/share/udhcpc/default.script SDLBase/board/novasis/NOVAsomS/Init/usr/share/udhcpc/default.script
--- buildroot-2016.05/board/novasis/NOVAsomS/Init/usr/share/udhcpc/default.script	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/Init/usr/share/udhcpc/default.script	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,83 @@
+#!/bin/sh
+
+# udhcpc script edited by Tim Riker <Tim@Rikers.org>
+
+[ -z "$1" ] && echo "Error: should be called from udhcpc" && exit 1
+
+RESOLV_CONF="/etc/resolv.conf"
+[ -e $RESOLV_CONF ] || touch $RESOLV_CONF
+[ -n "$broadcast" ] && BROADCAST="broadcast $broadcast"
+[ -n "$subnet" ] && NETMASK="netmask $subnet"
+
+case "$1" in
+	deconfig)
+		/sbin/ifconfig $interface up
+		/sbin/ifconfig $interface 0.0.0.0
+
+		# drop info from this interface
+		# resolv.conf may be a symlink to /tmp/, so take care
+		TMPFILE=$(mktemp)
+		grep -vE "# $interface\$" $RESOLV_CONF > $TMPFILE
+		cat $TMPFILE > $RESOLV_CONF
+		rm -f $TMPFILE
+
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -k $interface
+		fi
+		;;
+
+	leasefail|nak)
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -wD $interface --no-chroot
+		fi
+		;;
+
+	renew|bound)
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -k $interface
+		fi
+		/sbin/ifconfig $interface $ip $BROADCAST $NETMASK
+
+		if [ -n "$router" ] ; then
+			echo "deleting routers"
+			while route del default gw 0.0.0.0 dev $interface 2> /dev/null; do
+				:
+			done
+
+			for i in $router ; do
+				route add default gw $i dev $interface
+			done
+		fi
+
+		# drop info from this interface
+		# resolv.conf may be a symlink to /tmp/, so take care
+		TMPFILE=$(mktemp)
+		grep -vE "# $interface\$" $RESOLV_CONF > $TMPFILE
+		cat $TMPFILE > $RESOLV_CONF
+		rm -f $TMPFILE
+
+		# prefer rfc3359 domain search list (option 119) if available
+		if [ -n "$search" ]; then
+			search_list=$search
+		elif [ -n "$domain" ]; then
+			search_list=$domain
+		fi
+
+		[ -n "$search_list" ] &&
+			echo "search $search_list # $interface" >> $RESOLV_CONF
+
+		for i in $dns ; do
+			echo adding dns $i
+			echo "nameserver $i # $interface" >> $RESOLV_CONF
+		done
+                /etc/init.d/afterlease_services
+		;;
+esac
+
+HOOK_DIR="$0.d"
+for hook in "${HOOK_DIR}/"*; do
+    [ -f "${hook}" -a -x "${hook}" ] || continue
+    "${hook}" "${@}"
+done
+
+exit 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/post-image.sh SDLBase/board/novasis/NOVAsomS/post-image.sh
--- buildroot-2016.05/board/novasis/NOVAsomS/post-image.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/post-image.sh	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+#!/bin/bash
+
+echo "###### Creating uInitrd for NOVAsomS ######"
+output/host/usr/bin/mkimage -A arm -O linux -T ramdisk -C none -n "NOVAsomS" -a 0x80000 -d output/images/rootfs.ext3.gz output/images/uInitrd
+pushd ../../Deploy
+rm uInitrd
+ln -s ${BASE_DIR}/images/uInitrd
+popd
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/pre-image.sh SDLBase/board/novasis/NOVAsomS/pre-image.sh
--- buildroot-2016.05/board/novasis/NOVAsomS/pre-image.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/pre-image.sh	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+#!/bin/bash
+
+echo "###### Customizing file system ######"
+echo "cp -r board/novasis/NOVAsomS/Init/* ${TARGET_DIR}"
+cp -r board/novasis/NOVAsomS/Init/* ${TARGET_DIR}
+chmod 777 ${TARGET_DIR}/bin/*
+chmod 777 ${TARGET_DIR}/etc/init.d/*
+sync
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomS/readme.txt SDLBase/board/novasis/NOVAsomS/readme.txt
--- buildroot-2016.05/board/novasis/NOVAsomS/readme.txt	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomS/readme.txt	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,29 @@
+***************************
+Novasis NOVAsomP board
+***************************
+
+This file documents the Buildroot support for the NOVAsomP board.
+
+Please read the Quick Start Guide [1] for an introduction to the board.
+
+Build
+=====
+
+Configure Buildroot for your i.MX6UL EVK board:
+
+  make novasis_novasomp_defconfig
+
+Build all components:
+
+  make
+
+You will find in ./output/images/ the following files:
+  - rootfs.ext2
+  - rootfs.tar
+  - uInitrd
+
+Enjoy!
+
+References
+==========
+[1] http://www.novasis.it/NOVAsomP.html
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/genimage.cfg SDLBase/board/novasis/NOVAsomU/genimage.cfg
--- buildroot-2016.05/board/novasis/NOVAsomU/genimage.cfg	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/genimage.cfg	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,41 @@
+# Minimal microSD card image for the Freescale's i.MX6UL EVK board
+#
+# We mimic the .sdcard Freescale's image format for i.MX6UL:
+# * the microSD card must have 1 kB free space at the beginning,
+# * U-Boot is dumped as is,
+# * a FAT partition at offset 8 MB is containing zImage and dtbs,
+# * a single root filesystem partition is required (Ext2 in this case).
+#
+
+image boot.vfat {
+  vfat {
+    files = {
+      "imx6ul-14x14-evk.dtb",
+      "zImage"
+    }
+  }
+  size = 16M
+}
+
+image sdcard.img {
+  hdimage {
+  }
+
+  partition u-boot {
+    in-partition-table = "no"
+    image = "u-boot.imx"
+    offset = 1024
+  }
+
+  partition boot {
+    partition-type = 0xC
+    bootable = "true"
+    image = "boot.vfat"
+    offset = 8M
+  }
+
+  partition rootfs {
+    partition-type = 0x83
+    image = "rootfs.ext2"
+  }
+}
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/autologin.sh SDLBase/board/novasis/NOVAsomU/Init/bin/autologin.sh
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/autologin.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/bin/autologin.sh	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,2 @@
+#!/bin/sh
+/bin/login -f root
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/get_exec SDLBase/board/novasis/NOVAsomU/Init/bin/get_exec
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/get_exec	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/bin/get_exec	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,10 @@
+#!/bin/sh
+. /etc/sysconfig/system_vars
+if [ "$1" == "" ]; then
+        echo "Usage `basename $0` <filename>"
+        exit 0
+fi
+cd /tmp
+rm -f `basename ${1}`
+wget http://${REFERENCE_SERVER}/${1}
+chmod 777 `basename ${1}`
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/update_fs SDLBase/board/novasis/NOVAsomU/Init/bin/update_fs
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/update_fs	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/bin/update_fs	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,8 @@
+#!/bin/sh
+get_exec uInitrd       
+rm -rf /tmp/update_dir
+mkdir /tmp/update_dir
+mount /dev/mmcblk0p1 /tmp/update_dir
+cp /tmp/uInitrd /tmp/update_dir/.       
+umount /tmp/update_dir
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/update_kernel SDLBase/board/novasis/NOVAsomU/Init/bin/update_kernel
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/bin/update_kernel	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/bin/update_kernel	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,10 @@
+#!/bin/sh
+get_exec zImage
+get_exec imx6ul-14x14-evk.dtb
+rm -rf /tmp/update_dir
+mkdir /tmp/update_dir
+mount /dev/mmcblk1p1 /tmp/update_dir
+cp /tmp/zImage /tmp/update_dir/.       
+cp /tmp/imx6ul-14x14-evk.dtb /tmp/update_dir/.
+umount /tmp/update_dir
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/fstab SDLBase/board/novasis/NOVAsomU/Init/etc/fstab
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/fstab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/fstab	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,8 @@
+# <file system>	<mount pt>	<type>	<options>	<dump>	<pass>
+/dev/root	/		ext2	rw,noauto	0	1
+proc		/proc		proc	defaults	0	0
+devpts		/dev/pts	devpts	defaults,gid=5,mode=620	0	0
+tmpfs		/dev/shm	tmpfs	mode=0777	0	0
+tmpfs		/tmp		tmpfs	mode=1777	0	0
+tmpfs		/run		tmpfs	mode=0755,nosuid,nodev	0	0
+sysfs		/sys		sysfs	defaults	0	0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/group SDLBase/board/novasis/NOVAsomU/Init/etc/group
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/group	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/group	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,26 @@
+root:x:0:
+daemon:x:1:
+bin:x:2:
+sys:x:3:
+adm:x:4:
+tty:x:5:
+disk:x:6:
+lp:x:7:
+mail:x:8:
+kmem:x:9:
+wheel:x:10:root
+cdrom:x:11:
+dialout:x:18:
+floppy:x:19:
+video:x:28:
+audio:x:29:
+tape:x:32:
+www-data:x:33:
+operator:x:37:
+utmp:x:43:
+plugdev:x:46:
+staff:x:50:
+lock:x:54:
+netdev:x:82:
+nogroup:x:99:
+users:x:100:
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_S49ntp SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_S49ntp
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_S49ntp	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_S49ntp	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,57 @@
+#! /bin/sh
+#
+# System-V init script for the openntp daemon
+#
+
+. /etc/sysconfig/network
+. /etc/sysconfig/wireless
+
+[ ! -f /usr/sbin/ntpd ] && return 0
+COUNT=0
+/sbin/hwclock -r
+case "$1" in
+  start)
+        echo "Starting ntpd"
+        IP=`ifconfig | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+        if [ "${WLAN_ENABLED}" = "Y" ]; then
+                while [ "$IP" == "" ]; do
+                        echo "Waiting for IP"
+                        sleep 1
+                        IP=`ifconfig ${WLAN_DEVICE} | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+                        let COUNT=$COUNT+1
+                        if [ ${COUNT} -ge 10 ]; then
+                                break
+                        fi
+                done
+                DEVICE=${WLAN_DEVICE}
+        fi
+        if [ "${ETH0_ENABLED}" = "Y" ]; then
+                while [ "$IP" == "" ]; do
+                        echo "Waiting for IP"
+                        sleep 1
+                        IP=`ifconfig ${NET_DEVICE} | grep Mask | awk '{print $2}' | sed 's/addr://g'`
+                        let COUNT=$COUNT+1
+                        if [ ${COUNT} -ge 10 ]; then
+                                break
+                        fi
+                done
+                DEVICE=${NET_DEVICE}
+        fi
+        /usr/sbin/ntpd -g -I ${DEVICE}
+        /etc/init.d/hwclock_updater &
+        ;;
+  stop) echo "Stopping ntpd"
+        kill -9 `pidof ntpd`
+        ;;
+  restart) echo "Restarting ntpd"
+        $0 stop
+        sleep 1
+        $0 start
+        ;;
+  *) echo "Usage: $0 {start|stop|restart|reload|force-reload}" >&2
+        exit 1
+        ;;
+esac
+
+exit 0
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_services SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_services
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_services	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/afterlease_services	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,17 @@
+#!/bin/sh
+# called by usr/share/udhcpc/default.script after the dhcp have a valid ip
+
+. /etc/sysconfig/system_vars
+. /etc/sysconfig/network
+
+#PROGRESS=`cat /tmp/progress`
+for i in /etc/init.d/afterlease_S??* ;do
+	[ ! -f "$i" ] && continue
+	$i start
+done
+
+ifconfig | grep Bcast | awk '{print $2}' | sed 's/addr://g' > /tmp/my_ip
+ifconfig lo up
+sleep 1
+touch /tmp/system_ready
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/hwclock_updater SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/hwclock_updater
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/hwclock_updater	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/hwclock_updater	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,4 @@
+#!/bin/sh
+sleep 20
+/sbin/hwclock -w
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/rcK SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/rcK
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/rcK	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/rcK	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,27 @@
+#!/bin/sh
+
+
+# Stop all init scripts in /etc/init.d
+# executing them in reversed numerical order.
+#
+for i in $(ls -r /etc/init.d/S??*) ;do
+
+     # Ignore dangling symlinks (if any).
+     [ ! -f "$i" ] && continue
+
+     case "$i" in
+	*.sh)
+	    # Source shell script for speed.
+	    (
+		trap - INT QUIT TSTP
+		set stop
+		. $i
+	    )
+	    ;;
+	*)
+	    # No sh extension, so fork subprocess.
+	    $i stop
+	    ;;
+    esac
+done
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/rcS SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/rcS
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/rcS	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/rcS	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,48 @@
+#!/bin/sh
+
+mount /dev/mmcblk0p3 /mnt
+if [ -d /mnt/sysconfig ]; then
+	# If the file REINIT_VARS is present remove all saved and restart with default values
+	if [ -f /mnt/sysconfig/REINIT_VARS ]; then
+		cp -a /etc /mnt/sysconfig/.
+		cp -a /root /mnt/sysconfig/.
+		rm /mnt/sysconfig/REINIT_VARS
+	else
+		if [ -d /mnt/sysconfig/etc ]; then
+			cp -a /mnt/sysconfig/etc /
+		fi
+
+		if [ -d /mnt/sysconfig/root ]; then
+			cp -a /mnt/sysconfig/root /
+			cp -a /mnt/sysconfig/root /
+		fi
+	fi
+else
+	mkdir /mnt/sysconfig
+	cp -a /etc /mnt/sysconfig/.
+	cp -a /root /mnt/sysconfig/.
+fi
+umount /mnt
+
+# Start all init scripts in /etc/init.d
+# executing them in numerical order.
+#
+for i in /etc/init.d/S??* ;do
+	# Ignore dangling symlinks (if any).
+	[ ! -f "$i" ] && continue
+
+	case "$i" in
+		*.sh)
+			# Source shell script for speed.
+			(
+				trap - INT QUIT TSTP
+				set start
+				. $i
+			)
+			;;
+		*)
+			# No sh extension, so fork subprocess.
+			$i start
+			;;
+	esac
+done
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S01logging SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S01logging
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S01logging	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S01logging	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,40 @@
+#!/bin/sh
+#
+# Start logging
+#
+
+SYSLOGD_ARGS=-n
+KLOGD_ARGS=-n
+[ -r /etc/default/logging ] && . /etc/default/logging
+
+start() {
+	printf "Starting logging: "
+	start-stop-daemon -b -S -q -m -p /var/run/syslogd.pid --exec /sbin/syslogd -- $SYSLOGD_ARGS
+	start-stop-daemon -b -S -q -m -p /var/run/klogd.pid --exec /sbin/klogd -- $KLOGD_ARGS
+	echo "OK"
+}
+
+stop() {
+	printf "Stopping logging: "
+	start-stop-daemon -K -q -p /var/run/syslogd.pid
+	start-stop-daemon -K -q -p /var/run/klogd.pid
+	echo "OK"
+}
+
+case "$1" in
+  start)
+	start
+	;;
+  stop)
+	stop
+	;;
+  restart|reload)
+	stop
+	start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S20urandom SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S20urandom
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S20urandom	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S20urandom	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,51 @@
+#! /bin/sh
+#
+# urandom	This script saves the random seed between reboots.
+#		It is called from the boot, halt and reboot scripts.
+#
+# Version:	@(#)urandom  1.33  22-Jun-1998  miquels@cistron.nl
+#
+
+[ -c /dev/urandom ] || exit 0
+#. /etc/default/rcS
+
+case "$1" in
+	start|"")
+		# check for read only file system
+		if ! touch /etc/random-seed 2>/dev/null
+		then
+			echo "read-only file system detected...done"
+			exit
+		fi
+		if [ "$VERBOSE" != no ]
+		then
+			printf "Initializing random number generator... "
+		fi
+		# Load and then save 512 bytes,
+		# which is the size of the entropy pool
+		cat /etc/random-seed >/dev/urandom
+		rm -f /etc/random-seed
+		umask 077
+		dd if=/dev/urandom of=/etc/random-seed count=1 \
+			>/dev/null 2>&1 || echo "urandom start: failed."
+		umask 022
+		[ "$VERBOSE" != no ] && echo "done."
+		;;
+	stop)
+		if ! touch /etc/random-seed 2>/dev/null
+                then
+                        exit
+                fi
+		# Carry a random seed from shut-down to start-up;
+		# see documentation in linux/drivers/char/random.c
+		[ "$VERBOSE" != no ] && printf "Saving random seed... "
+		umask 077
+		dd if=/dev/urandom of=/etc/random-seed count=1 \
+			>/dev/null 2>&1 || echo "urandom stop: failed."
+		[ "$VERBOSE" != no ] && echo "done."
+		;;
+	*)
+		echo "Usage: urandom {start|stop}" >&2
+		exit 1
+		;;
+esac
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S40network SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S40network
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S40network	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S40network	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,56 @@
+#!/bin/sh
+#
+# Start the network....
+#
+
+. /etc/sysconfig/system_vars
+. /etc/sysconfig/network
+
+[ "${ETH0_ENABLED}" = "N" ] && exit 0
+
+MAC_FROM_CMDLINE="00:4f:9f:01:00:0e"
+for MACADDR in `cat /proc/cmdline` ; do
+        echo $MACADDR | grep mac_addr > /dev/null
+        [ "$?" == "0" ] && MAC_FROM_CMDLINE=`echo "${MACADDR}" | sed 's/mac_addr=//g'`
+done
+
+if [ -f /etc/sysconfig/mac_addr ];  then
+        . /etc/sysconfig/mac_addr
+fi
+
+case "$1" in
+  start)
+ 	echo "Starting network..."
+	if [ "$NET_USE_DHCP" == "Y" ]; then
+		ifconfig ${NET_DEVICE} hw ether ${MAC_FROM_CMDLINE} up
+		udhcpc -i ${NET_DEVICE} &
+	else
+		echo "search ${NET_SUFFIX}" > /etc/resolv.conf
+		echo "nameserver ${NET_DNS_LIST}" >> /etc/resolv.conf
+		ifconfig ${NET_DEVICE} ${NET_IP_ADDRESS} netmask ${NET_MASK} hw ether ${MAC_FROM_CMDLINE} up >/dev/null 2>&1
+		route add default gw ${NET_GATEWAY} dev ${NET_DEVICE} >/dev/null 2>&1
+		echo "${NET_IP_ADDRESS}" > /tmp/my_ip
+	fi
+	if [ -f /usr/sbin/telnetd ]; then
+		telnetd
+	fi
+	if [ -f /usr/sbin/inetd ]; then
+		inetd
+	fi
+	;;
+  stop)
+	echo -n "Stopping network..."
+	/sbin/ifdown -a
+	kill -9 `pidof telnetd`
+	;;
+  restart|reload)
+	"$0" stop
+	"$0" start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S49ntp SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S49ntp
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S49ntp	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S49ntp	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,6 @@
+#! /bin/sh
+#
+# Empty, used after network is up
+#
+
+exit 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S50sshd SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S50sshd
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S50sshd	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S50sshd	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,57 @@
+#!/bin/sh
+#
+# sshd        Starts sshd.
+#
+
+# Make sure the ssh-keygen progam exists
+[ -f /usr/bin/ssh-keygen ] || exit 0
+
+mount /dev/mmcblk0p3 /mnt
+if [ -d /mnt/sysconfig/ssh ]; then
+	cp -r /mnt/sysconfig/ss* /etc/.
+else
+	# Create any missing keys
+	/usr/bin/ssh-keygen -A
+	if [ ! -d /mnt/sysconfig ]; then
+		mkdir /mnt/sysconfig
+	fi
+	cp -r /etc/ss* /mnt/sysconfig/.
+fi
+umount /mnt
+
+umask 077
+
+start() {
+	echo -n "Starting sshd: "
+	/usr/sbin/sshd
+	touch /var/lock/sshd
+	echo "OK"
+}
+stop() {
+	echo -n "Stopping sshd: "
+	killall sshd
+	rm -f /var/lock/sshd
+	echo "OK"
+}
+restart() {
+	stop
+	start
+}
+
+case "$1" in
+  start)
+	start
+	;;
+  stop)
+	stop
+	;;
+  restart|reload)
+	restart
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S50telnet SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S50telnet
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S50telnet	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S50telnet	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,36 @@
+#!/bin/sh
+#
+# Start telnet....
+#
+
+start() {
+      printf "Starting telnetd: "
+      start-stop-daemon -S -q -m -b -p /var/run/telnetd.pid \
+			-x /usr/sbin/telnetd -- -F
+      [ $? = 0 ] && echo "OK" || echo "FAIL"
+}
+
+stop() {
+	printf "Stopping telnetd: "
+	start-stop-daemon -K -q -p /var/run/telnetd.pid \
+			  -x /usr/sbin/telnetd
+	[ $? = 0 ] && echo "OK" || echo "FAIL"
+}
+
+case "$1" in
+    start)
+	start
+	;;
+    stop)
+	stop
+	;;
+    restart|reload)
+	stop
+	start
+	;;
+  *)
+	echo "Usage: $0 {start|stop|restart}"
+	exit 1
+esac
+
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S97mysqld SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S97mysqld
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/init.d/S97mysqld	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/init.d/S97mysqld	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,38 @@
+#!/bin/sh
+[ ! -f /usr/bin/mysql ] && exit 0
+
+case "$1" in
+	start)
+		if [ ! -d /tmp/mysql ] ; then
+			echo "Creating MySQL system tables..."
+			mysql_install_db --user=mysql --ldata=/tmp/mysql
+		fi
+
+		# We don't use start-stop-daemon because mysqld has
+		# its own wrapper script.
+		echo -n "Starting mysql..."
+		/usr/bin/mysqld_safe --pid-file=/var/run/mysqld.pid  --datadir=/tmp/mysql &
+		usleep 900000
+mysql << _EOF_
+GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' IDENTIFIED BY 'novasis';
+quit
+_EOF_
+
+
+		echo "done."
+		;;
+	stop)
+		echo -n "Stopping mysql..."
+		if test -f /var/run/mysqld.pid ; then
+			kill `cat /var/run/mysqld.pid`
+		fi
+		echo "done."
+		;;
+	restart)
+		$0 stop
+		$0 start
+		;;
+	*)
+		echo "Usage: /etc/init.d/mysqld {start|stop|restart}"
+		;;
+esac
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/inittab SDLBase/board/novasis/NOVAsomU/Init/etc/inittab
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/inittab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/inittab	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,35 @@
+# /etc/inittab
+#
+# Copyright (C) 2001 Erik Andersen <andersen@codepoet.org>
+#
+# Note: BusyBox init doesn't support runlevels.  The runlevels field is
+# completely ignored by BusyBox init. If you want runlevels, use
+# sysvinit.
+#
+# Format for each entry: <id>:<runlevels>:<action>:<process>
+#
+# id        == tty to run on, or empty for /dev/console
+# runlevels == ignored
+# action    == one of sysinit, respawn, askfirst, wait, and once
+# process   == program to run
+
+# Startup the system
+::sysinit:/bin/mount -t proc proc /proc
+::sysinit:/bin/mount -o remount,rw /
+::sysinit:/bin/mkdir -p /dev/pts
+::sysinit:/bin/mkdir -p /dev/shm
+::sysinit:/bin/mount -a
+::sysinit:/bin/hostname -F /etc/hostname
+# now run any rc scripts
+::sysinit:/etc/init.d/rcS
+
+# Put a getty on the serial port
+ttymxc0::respawn:/sbin/getty -L -l /bin/autologin.sh -n ttymxc0 115200 vt100 # GENERIC_SERIAL
+
+# Stuff to do for the 3-finger salute
+#::ctrlaltdel:/sbin/reboot
+
+# Stuff to do before rebooting
+::shutdown:/etc/init.d/rcK
+::shutdown:/sbin/swapoff -a
+::shutdown:/bin/umount -a -r
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/issue SDLBase/board/novasis/NOVAsomU/Init/etc/issue
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/issue	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/issue	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1 @@
+Welcome
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/mtab SDLBase/board/novasis/NOVAsomU/Init/etc/mtab
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/mtab	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/mtab	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,30 @@
+sysfs /sys sysfs rw,nosuid,nodev,noexec,relatime 0 0
+proc /proc proc rw,nosuid,nodev,noexec,relatime 0 0
+udev /dev devtmpfs rw,nosuid,relatime,size=4971252k,nr_inodes=1242813,mode=755 0 0
+devpts /dev/pts devpts rw,nosuid,noexec,relatime,gid=5,mode=620,ptmxmode=000 0 0
+tmpfs /run tmpfs rw,nosuid,noexec,relatime,size=998148k,mode=755 0 0
+/dev/sda1 / ext4 rw,relatime,errors=remount-ro,data=ordered 0 0
+securityfs /sys/kernel/security securityfs rw,nosuid,nodev,noexec,relatime 0 0
+tmpfs /dev/shm tmpfs rw,nosuid,nodev 0 0
+tmpfs /run/lock tmpfs rw,nosuid,nodev,noexec,relatime,size=5120k 0 0
+tmpfs /sys/fs/cgroup tmpfs ro,nosuid,nodev,noexec,mode=755 0 0
+cgroup /sys/fs/cgroup/systemd cgroup rw,nosuid,nodev,noexec,relatime,xattr,release_agent=/lib/systemd/systemd-cgroups-agent,name=systemd 0 0
+pstore /sys/fs/pstore pstore rw,nosuid,nodev,noexec,relatime 0 0
+cgroup /sys/fs/cgroup/net_cls,net_prio cgroup rw,nosuid,nodev,noexec,relatime,net_cls,net_prio 0 0
+cgroup /sys/fs/cgroup/pids cgroup rw,nosuid,nodev,noexec,relatime,pids 0 0
+cgroup /sys/fs/cgroup/cpu,cpuacct cgroup rw,nosuid,nodev,noexec,relatime,cpu,cpuacct 0 0
+cgroup /sys/fs/cgroup/devices cgroup rw,nosuid,nodev,noexec,relatime,devices 0 0
+cgroup /sys/fs/cgroup/hugetlb cgroup rw,nosuid,nodev,noexec,relatime,hugetlb 0 0
+cgroup /sys/fs/cgroup/perf_event cgroup rw,nosuid,nodev,noexec,relatime,perf_event 0 0
+cgroup /sys/fs/cgroup/freezer cgroup rw,nosuid,nodev,noexec,relatime,freezer 0 0
+cgroup /sys/fs/cgroup/cpuset cgroup rw,nosuid,nodev,noexec,relatime,cpuset 0 0
+cgroup /sys/fs/cgroup/memory cgroup rw,nosuid,nodev,noexec,relatime,memory 0 0
+cgroup /sys/fs/cgroup/blkio cgroup rw,nosuid,nodev,noexec,relatime,blkio 0 0
+systemd-1 /proc/sys/fs/binfmt_misc autofs rw,relatime,fd=26,pgrp=1,timeout=0,minproto=5,maxproto=5,direct 0 0
+debugfs /sys/kernel/debug debugfs rw,relatime 0 0
+hugetlbfs /dev/hugepages hugetlbfs rw,relatime 0 0
+mqueue /dev/mqueue mqueue rw,relatime 0 0
+fusectl /sys/fs/fuse/connections fusectl rw,relatime 0 0
+UShared /media/sf_UShared vboxsf rw,nodev,relatime 0 0
+tmpfs /run/user/1000 tmpfs rw,nosuid,nodev,relatime,size=998148k,mode=700,uid=1000,gid=1000 0 0
+gvfsd-fuse /run/user/1000/gvfs fuse.gvfsd-fuse rw,nosuid,nodev,relatime,user_id=1000,group_id=1000 0 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/network/if-pre-up.d/wait_iface SDLBase/board/novasis/NOVAsomU/Init/etc/network/if-pre-up.d/wait_iface
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/network/if-pre-up.d/wait_iface	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/network/if-pre-up.d/wait_iface	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,21 @@
+#!/bin/sh
+
+# In case we have a slow-to-appear interface (e.g. eth-over-USB),
+# and we need to configure it, wait until it appears, but not too
+# long either. IF_WAIT_DELAY is in seconds.
+
+if [ "${IF_WAIT_DELAY}" -a ! -e "/sys/class/net/${IFACE}" ]; then
+    printf "Waiting for interface %s to appear" "${IFACE}"
+    while [ ${IF_WAIT_DELAY} -gt 0 ]; do
+        if [ -e "/sys/class/net/${IFACE}" ]; then
+            printf "\n"
+            exit 0
+        fi
+        sleep 1
+        printf "."
+        : $((IF_WAIT_DELAY -= 1))
+    done
+    printf " timeout!\n"
+    exit 1
+fi
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/network/interfaces SDLBase/board/novasis/NOVAsomU/Init/etc/network/interfaces
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/network/interfaces	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/network/interfaces	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+# interface file auto-generated by buildroot
+
+auto lo
+iface lo inet loopback
+
+auto eth0
+iface eth0 inet dhcp
+	pre-up /etc/network/nfs_check
+	wait-delay 15
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/network/nfs_check SDLBase/board/novasis/NOVAsomU/Init/etc/network/nfs_check
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/network/nfs_check	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/network/nfs_check	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,20 @@
+#!/bin/sh
+
+# This allows NFS booting to work while also being able to configure
+# the network interface via DHCP when not NFS booting.  Otherwise, a
+# NFS booted system will likely hang during DHCP configuration.
+
+# Attempting to configure the network interface used for NFS will
+# initially bring that network down.  Since the root filesystem is
+# accessed over this network, the system hangs.
+
+# This script is run by ifup and will attempt to detect if a NFS root
+# mount uses the interface to be configured (IFACE), and if so does
+# not configure it.  This should allow the same build to be disk/flash
+# booted or NFS booted.
+
+nfsip=`sed -n '/^[^ ]*:.* \/ nfs.*[ ,]addr=\([0-9.]\+\).*/s//\1/p' /proc/mounts`
+if [ -n "$nfsip" ] && ip route get to "$nfsip" | grep -q "dev $IFACE"; then
+	echo Skipping $IFACE, used for NFS from $nfsip
+	exit 1
+fi
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/nsswitch.conf SDLBase/board/novasis/NOVAsomU/Init/etc/nsswitch.conf
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/nsswitch.conf	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/nsswitch.conf	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,13 @@
+# /etc/nsswitch.conf
+
+passwd:         files
+group:          files
+shadow:         files
+
+hosts:          files dns
+networks:       files dns
+
+protocols:      files
+services:       files
+ethers:         files
+rpc:            files
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/os-release SDLBase/board/novasis/NOVAsomU/Init/etc/os-release
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/os-release	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/os-release	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,5 @@
+NAME=Buildroot
+VERSION=2016.05
+ID=buildroot
+VERSION_ID=2016.05
+PRETTY_NAME="Buildroot 2016.05"
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/passwd SDLBase/board/novasis/NOVAsomU/Init/etc/passwd
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/passwd	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/passwd	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+root:x:0:0:root:/root:/bin/sh
+daemon:x:1:1:daemon:/usr/sbin:/bin/false
+bin:x:2:2:bin:/bin:/bin/false
+sys:x:3:3:sys:/dev:/bin/false
+sync:x:4:100:sync:/bin:/bin/sync
+mail:x:8:8:mail:/var/spool/mail:/bin/false
+www-data:x:33:33:www-data:/var/www:/bin/false
+operator:x:37:37:Operator:/var:/bin/false
+nobody:x:99:99:nobody:/home:/bin/false
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/profile SDLBase/board/novasis/NOVAsomU/Init/etc/profile
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/profile	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/profile	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,20 @@
+export PATH=/bin:/sbin:/usr/bin:/usr/sbin
+
+if [ "$PS1" ]; then
+	if [ "`id -u`" -eq 0 ]; then
+		export PS1='# '
+	else
+		export PS1='$ '
+	fi
+fi
+
+export PAGER='/bin/more '
+export EDITOR='/bin/vi'
+
+# Source configuration files from /etc/profile.d
+for i in /etc/profile.d/*.sh ; do
+	if [ -r "$i" ]; then
+		. $i
+	fi
+	unset i
+done
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/profile.d/umask.sh SDLBase/board/novasis/NOVAsomU/Init/etc/profile.d/umask.sh
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/profile.d/umask.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/profile.d/umask.sh	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1 @@
+umask 022
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/protocols SDLBase/board/novasis/NOVAsomU/Init/etc/protocols
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/protocols	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/protocols	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,61 @@
+# Internet (IP) protocols
+#
+# Updated from http://www.iana.org/assignments/protocol-numbers and other
+# sources.
+
+ip	0	IP		# internet protocol, pseudo protocol number
+hopopt	0	HOPOPT		# IPv6 Hop-by-Hop Option [RFC1883]
+icmp	1	ICMP		# internet control message protocol
+igmp	2	IGMP		# Internet Group Management
+ggp	3	GGP		# gateway-gateway protocol
+ipencap	4	IP-ENCAP	# IP encapsulated in IP (officially ``IP'')
+st	5	ST		# ST datagram mode
+tcp	6	TCP		# transmission control protocol
+egp	8	EGP		# exterior gateway protocol
+igp	9	IGP		# any private interior gateway (Cisco)
+pup	12	PUP		# PARC universal packet protocol
+udp	17	UDP		# user datagram protocol
+hmp	20	HMP		# host monitoring protocol
+xns-idp	22	XNS-IDP		# Xerox NS IDP
+rdp	27	RDP		# "reliable datagram" protocol
+iso-tp4	29	ISO-TP4		# ISO Transport Protocol class 4 [RFC905]
+dccp	33	DCCP		# Datagram Congestion Control Prot. [RFC4340]
+xtp	36	XTP		# Xpress Transfer Protocol
+ddp	37	DDP		# Datagram Delivery Protocol
+idpr-cmtp 38	IDPR-CMTP	# IDPR Control Message Transport
+ipv6	41	IPv6		# Internet Protocol, version 6
+ipv6-route 43	IPv6-Route	# Routing Header for IPv6
+ipv6-frag 44	IPv6-Frag	# Fragment Header for IPv6
+idrp	45	IDRP		# Inter-Domain Routing Protocol
+rsvp	46	RSVP		# Reservation Protocol
+gre	47	GRE		# General Routing Encapsulation
+esp	50	IPSEC-ESP	# Encap Security Payload [RFC2406]
+ah	51	IPSEC-AH	# Authentication Header [RFC2402]
+skip	57	SKIP		# SKIP
+ipv6-icmp 58	IPv6-ICMP	# ICMP for IPv6
+ipv6-nonxt 59	IPv6-NoNxt	# No Next Header for IPv6
+ipv6-opts 60	IPv6-Opts	# Destination Options for IPv6
+rspf	73	RSPF CPHB	# Radio Shortest Path First (officially CPHB)
+vmtp	81	VMTP		# Versatile Message Transport
+eigrp	88	EIGRP		# Enhanced Interior Routing Protocol (Cisco)
+ospf	89	OSPFIGP		# Open Shortest Path First IGP
+ax.25	93	AX.25		# AX.25 frames
+ipip	94	IPIP		# IP-within-IP Encapsulation Protocol
+etherip	97	ETHERIP		# Ethernet-within-IP Encapsulation [RFC3378]
+encap	98	ENCAP		# Yet Another IP encapsulation [RFC1241]
+#	99			# any private encryption scheme
+pim	103	PIM		# Protocol Independent Multicast
+ipcomp	108	IPCOMP		# IP Payload Compression Protocol
+vrrp	112	VRRP		# Virtual Router Redundancy Protocol [RFC5798]
+l2tp	115	L2TP		# Layer Two Tunneling Protocol [RFC2661]
+isis	124	ISIS		# IS-IS over IPv4
+sctp	132	SCTP		# Stream Control Transmission Protocol
+fc	133	FC		# Fibre Channel
+mobility-header 135 Mobility-Header # Mobility Support for IPv6 [RFC3775]
+udplite	136	UDPLite		# UDP-Lite [RFC3828]
+mpls-in-ip 137	MPLS-in-IP	# MPLS-in-IP [RFC4023]
+manet	138			# MANET Protocols [RFC5498]
+hip	139	HIP		# Host Identity Protocol
+shim6	140	Shim6		# Shim6 Protocol [RFC5533]
+wesp	141	WESP		# Wrapped Encapsulating Security Payload
+rohc	142	ROHC		# Robust Header Compression
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/services SDLBase/board/novasis/NOVAsomU/Init/etc/services
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/services	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/services	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,302 @@
+# /etc/services:
+# $Id: services,v 1.1 2004/10/09 02:49:18 andersen Exp $
+#
+# Network services, Internet style
+#
+# Note that it is presently the policy of IANA to assign a single well-known
+# port number for both TCP and UDP; hence, most entries here have two entries
+# even if the protocol doesn't support UDP operations.
+# Updated from RFC 1700, ``Assigned Numbers'' (October 1994).  Not all ports
+# are included, only the more common ones.
+
+tcpmux		1/tcp				# TCP port service multiplexer
+echo		7/tcp
+echo		7/udp
+discard		9/tcp		sink null
+discard		9/udp		sink null
+systat		11/tcp		users
+daytime		13/tcp
+daytime		13/udp
+netstat		15/tcp
+qotd		17/tcp		quote
+msp		18/tcp				# message send protocol
+msp		18/udp				# message send protocol
+chargen		19/tcp		ttytst source
+chargen		19/udp		ttytst source
+ftp-data	20/tcp
+ftp		21/tcp
+fsp		21/udp		fspd
+ssh		22/tcp				# SSH Remote Login Protocol
+ssh		22/udp				# SSH Remote Login Protocol
+telnet		23/tcp
+# 24 - private
+smtp		25/tcp		mail
+# 26 - unassigned
+time		37/tcp		timserver
+time		37/udp		timserver
+rlp		39/udp		resource	# resource location
+nameserver	42/tcp		name		# IEN 116
+whois		43/tcp		nicname
+re-mail-ck	50/tcp				# Remote Mail Checking Protocol
+re-mail-ck	50/udp				# Remote Mail Checking Protocol
+domain		53/tcp		nameserver	# name-domain server
+domain		53/udp		nameserver
+mtp		57/tcp				# deprecated
+bootps		67/tcp				# BOOTP server
+bootps		67/udp
+bootpc		68/tcp				# BOOTP client
+bootpc		68/udp
+tftp		69/udp
+gopher		70/tcp				# Internet Gopher
+gopher		70/udp
+rje		77/tcp		netrjs
+finger		79/tcp
+www		80/tcp		http		# WorldWideWeb HTTP
+www		80/udp				# HyperText Transfer Protocol
+link		87/tcp		ttylink
+kerberos	88/tcp		kerberos5 krb5	# Kerberos v5
+kerberos	88/udp		kerberos5 krb5	# Kerberos v5
+supdup		95/tcp
+# 100 - reserved
+hostnames	101/tcp		hostname	# usually from sri-nic
+iso-tsap	102/tcp		tsap		# part of ISODE.
+csnet-ns	105/tcp		cso-ns		# also used by CSO name server
+csnet-ns	105/udp		cso-ns
+# unfortunately the poppassd (Eudora) uses a port which has already
+# been assigned to a different service. We list the poppassd as an
+# alias here. This should work for programs asking for this service.
+# (due to a bug in inetd the 3com-tsmux line is disabled)
+#3com-tsmux	106/tcp		poppassd
+#3com-tsmux	106/udp		poppassd
+rtelnet		107/tcp				# Remote Telnet
+rtelnet		107/udp
+pop-2		109/tcp		postoffice	# POP version 2
+pop-2		109/udp
+pop-3		110/tcp				# POP version 3
+pop-3		110/udp
+sunrpc		111/tcp		portmapper	# RPC 4.0 portmapper TCP
+sunrpc		111/udp		portmapper	# RPC 4.0 portmapper UDP
+auth		113/tcp		authentication tap ident
+sftp		115/tcp
+uucp-path	117/tcp
+nntp		119/tcp		readnews untp	# USENET News Transfer Protocol
+ntp		123/tcp
+ntp		123/udp				# Network Time Protocol
+netbios-ns	137/tcp				# NETBIOS Name Service
+netbios-ns	137/udp
+netbios-dgm	138/tcp				# NETBIOS Datagram Service
+netbios-dgm	138/udp
+netbios-ssn	139/tcp				# NETBIOS session service
+netbios-ssn	139/udp
+imap2		143/tcp				# Interim Mail Access Proto v2
+imap2		143/udp
+snmp		161/udp				# Simple Net Mgmt Proto
+snmp-trap	162/udp		snmptrap	# Traps for SNMP
+cmip-man	163/tcp				# ISO mgmt over IP (CMOT)
+cmip-man	163/udp
+cmip-agent	164/tcp
+cmip-agent	164/udp
+xdmcp		177/tcp				# X Display Mgr. Control Proto
+xdmcp		177/udp
+nextstep	178/tcp		NeXTStep NextStep	# NeXTStep window
+nextstep	178/udp		NeXTStep NextStep	# server
+bgp		179/tcp				# Border Gateway Proto.
+bgp		179/udp
+prospero	191/tcp				# Cliff Neuman's Prospero
+prospero	191/udp
+irc		194/tcp				# Internet Relay Chat
+irc		194/udp
+smux		199/tcp				# SNMP Unix Multiplexer
+smux		199/udp
+at-rtmp		201/tcp				# AppleTalk routing
+at-rtmp		201/udp
+at-nbp		202/tcp				# AppleTalk name binding
+at-nbp		202/udp
+at-echo		204/tcp				# AppleTalk echo
+at-echo		204/udp
+at-zis		206/tcp				# AppleTalk zone information
+at-zis		206/udp
+qmtp		209/tcp				# The Quick Mail Transfer Protocol
+qmtp		209/udp				# The Quick Mail Transfer Protocol
+z3950		210/tcp		wais		# NISO Z39.50 database
+z3950		210/udp		wais
+ipx		213/tcp				# IPX
+ipx		213/udp
+imap3		220/tcp				# Interactive Mail Access
+imap3		220/udp				# Protocol v3
+ulistserv	372/tcp				# UNIX Listserv
+ulistserv	372/udp
+https		443/tcp				# MCom
+https		443/udp				# MCom
+snpp		444/tcp				# Simple Network Paging Protocol
+snpp		444/udp				# Simple Network Paging Protocol
+saft		487/tcp				# Simple Asynchronous File Transfer
+saft		487/udp				# Simple Asynchronous File Transfer
+npmp-local	610/tcp		dqs313_qmaster	# npmp-local / DQS
+npmp-local	610/udp		dqs313_qmaster	# npmp-local / DQS
+npmp-gui	611/tcp		dqs313_execd	# npmp-gui / DQS
+npmp-gui	611/udp		dqs313_execd	# npmp-gui / DQS
+hmmp-ind	612/tcp		dqs313_intercell# HMMP Indication / DQS
+hmmp-ind	612/udp		dqs313_intercell# HMMP Indication / DQS
+#
+# UNIX specific services
+#
+exec		512/tcp
+biff		512/udp		comsat
+login		513/tcp
+who		513/udp		whod
+shell		514/tcp		cmd		# no passwords used
+syslog		514/udp
+printer		515/tcp		spooler		# line printer spooler
+talk		517/udp
+ntalk		518/udp
+route		520/udp		router routed	# RIP
+timed		525/udp		timeserver
+tempo		526/tcp		newdate
+courier		530/tcp		rpc
+conference	531/tcp		chat
+netnews		532/tcp		readnews
+netwall		533/udp				# -for emergency broadcasts
+uucp		540/tcp		uucpd		# uucp daemon
+afpovertcp	548/tcp				# AFP over TCP
+afpovertcp	548/udp				# AFP over TCP
+remotefs	556/tcp		rfs_server rfs	# Brunhoff remote filesystem
+klogin		543/tcp				# Kerberized `rlogin' (v5)
+kshell		544/tcp		krcmd		# Kerberized `rsh' (v5)
+kerberos-adm	749/tcp				# Kerberos `kadmin' (v5)
+#
+webster		765/tcp				# Network dictionary
+webster		765/udp
+#
+# From ``Assigned Numbers'':
+#
+#> The Registered Ports are not controlled by the IANA and on most systems
+#> can be used by ordinary user processes or programs executed by ordinary
+#> users.
+#
+#> Ports are used in the TCP [45,106] to name the ends of logical
+#> connections which carry long term conversations.  For the purpose of
+#> providing services to unknown callers, a service contact port is
+#> defined.  This list specifies the port used by the server process as its
+#> contact port.  While the IANA can not control uses of these ports it
+#> does register or list uses of these ports as a convienence to the
+#> community.
+#
+nfsdstatus	1110/tcp
+nfsd-keepalive	1110/udp
+
+ingreslock	1524/tcp
+ingreslock	1524/udp
+prospero-np	1525/tcp			# Prospero non-privileged
+prospero-np	1525/udp
+datametrics	1645/tcp	old-radius	# datametrics / old radius entry
+datametrics	1645/udp	old-radius	# datametrics / old radius entry
+sa-msg-port	1646/tcp	old-radacct	# sa-msg-port / old radacct entry
+sa-msg-port	1646/udp	old-radacct	# sa-msg-port / old radacct entry
+radius		1812/tcp			# Radius
+radius		1812/udp			# Radius
+radacct		1813/tcp			# Radius Accounting
+radacct		1813/udp			# Radius Accounting
+nfsd		2049/tcp	nfs
+nfsd		2049/udp	nfs
+cvspserver	2401/tcp			# CVS client/server operations
+cvspserver	2401/udp			# CVS client/server operations
+mysql		3306/tcp			# MySQL
+mysql		3306/udp			# MySQL
+rfe		5002/tcp			# Radio Free Ethernet
+rfe		5002/udp			# Actually uses UDP only
+cfengine	5308/tcp			# CFengine
+cfengine	5308/udp			# CFengine
+bbs		7000/tcp			# BBS service
+#
+#
+# Kerberos (Project Athena/MIT) services
+# Note that these are for Kerberos v4, and are unofficial.  Sites running
+# v4 should uncomment these and comment out the v5 entries above.
+#
+kerberos4	750/udp		kerberos-iv kdc	# Kerberos (server) udp
+kerberos4	750/tcp		kerberos-iv kdc	# Kerberos (server) tcp
+kerberos_master	751/udp				# Kerberos authentication
+kerberos_master	751/tcp				# Kerberos authentication
+passwd_server	752/udp				# Kerberos passwd server
+krb_prop	754/tcp				# Kerberos slave propagation
+krbupdate	760/tcp		kreg		# Kerberos registration
+kpasswd		761/tcp		kpwd		# Kerberos "passwd"
+kpop		1109/tcp			# Pop with Kerberos
+knetd		2053/tcp			# Kerberos de-multiplexor
+zephyr-srv	2102/udp			# Zephyr server
+zephyr-clt	2103/udp			# Zephyr serv-hm connection
+zephyr-hm	2104/udp			# Zephyr hostmanager
+eklogin		2105/tcp			# Kerberos encrypted rlogin
+#
+# Unofficial but necessary (for NetBSD) services
+#
+supfilesrv	871/tcp				# SUP server
+supfiledbg	1127/tcp			# SUP debugging
+#
+# Datagram Delivery Protocol services
+#
+rtmp		1/ddp				# Routing Table Maintenance Protocol
+nbp		2/ddp				# Name Binding Protocol
+echo		4/ddp				# AppleTalk Echo Protocol
+zip		6/ddp				# Zone Information Protocol
+#
+# Services added for the Debian GNU/Linux distribution
+poppassd	106/tcp				# Eudora
+poppassd	106/udp				# Eudora
+mailq		174/tcp				# Mailer transport queue for Zmailer
+mailq		174/tcp				# Mailer transport queue for Zmailer
+omirr		808/tcp		omirrd		# online mirror
+omirr		808/udp		omirrd		# online mirror
+rmtcfg		1236/tcp			# Gracilis Packeten remote config server
+xtel		1313/tcp			# french minitel
+coda_opcons	1355/udp			# Coda opcons            (Coda fs)
+coda_venus	1363/udp			# Coda venus             (Coda fs)
+coda_auth	1357/udp			# Coda auth              (Coda fs)
+coda_udpsrv	1359/udp			# Coda udpsrv            (Coda fs)
+coda_filesrv	1361/udp			# Coda filesrv           (Coda fs)
+codacon		1423/tcp	venus.cmu	# Coda Console           (Coda fs)
+coda_aux1	1431/tcp			# coda auxiliary service (Coda fs)
+coda_aux1	1431/udp			# coda auxiliary service (Coda fs)
+coda_aux2	1433/tcp			# coda auxiliary service (Coda fs)
+coda_aux2	1433/udp			# coda auxiliary service (Coda fs)
+coda_aux3	1435/tcp			# coda auxiliary service (Coda fs)
+coda_aux3	1435/udp			# coda auxiliary service (Coda fs)
+cfinger		2003/tcp			# GNU Finger
+afbackup	2988/tcp			# Afbackup system
+afbackup	2988/udp			# Afbackup system
+icp		3130/tcp			# Internet Cache Protocol (Squid)
+icp		3130/udp			# Internet Cache Protocol (Squid)
+postgres	5432/tcp			# POSTGRES
+postgres	5432/udp			# POSTGRES
+fax		4557/tcp			# FAX transmission service        (old)
+hylafax		4559/tcp			# HylaFAX client-server protocol  (new)
+noclog		5354/tcp			# noclogd with TCP (nocol)
+noclog		5354/udp			# noclogd with UDP (nocol)
+hostmon		5355/tcp			# hostmon uses TCP (nocol)
+hostmon		5355/udp			# hostmon uses TCP (nocol)
+ircd		6667/tcp			# Internet Relay Chat
+ircd		6667/udp			# Internet Relay Chat
+webcache	8080/tcp			# WWW caching service
+webcache	8080/udp			# WWW caching service
+tproxy		8081/tcp			# Transparent Proxy
+tproxy		8081/udp			# Transparent Proxy
+mandelspawn	9359/udp	mandelbrot	# network mandelbrot
+amanda		10080/udp			# amanda backup services
+amandaidx	10082/tcp			# amanda backup services
+amidxtape	10083/tcp			# amanda backup services
+isdnlog		20011/tcp			# isdn logging system
+isdnlog		20011/udp			# isdn logging system
+vboxd		20012/tcp			# voice box system
+vboxd		20012/udp			# voice box system
+binkp           24554/tcp			# Binkley
+binkp           24554/udp			# Binkley
+asp		27374/tcp			# Address Search Protocol
+asp		27374/udp			# Address Search Protocol
+tfido           60177/tcp			# Ifmail
+tfido           60177/udp			# Ifmail
+fido            60179/tcp			# Ifmail
+fido            60179/udp			# Ifmail
+
+# Local services
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/shadow SDLBase/board/novasis/NOVAsomU/Init/etc/shadow
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/shadow	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/shadow	2017-01-10 14:12:55.009032694 +0100
@@ -0,0 +1,9 @@
+root::10933:0:99999:7:::
+daemon:*:10933:0:99999:7:::
+bin:*:10933:0:99999:7:::
+sys:*:10933:0:99999:7:::
+sync:*:10933:0:99999:7:::
+mail:*:10933:0:99999:7:::
+www-data:*:10933:0:99999:7:::
+operator:*:10933:0:99999:7:::
+nobody:*:10933:0:99999:7:::
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/sysconfig/network SDLBase/board/novasis/NOVAsomU/Init/etc/sysconfig/network
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/sysconfig/network	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/sysconfig/network	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,19 @@
+# Wired ethernet
+ETH0_ENABLED=Y
+NET_DEVICE=eth0
+NET_SUFFIX=novasis.it
+# Maybe N or Y
+# NET_USE_DHCP=N
+NET_USE_DHCP=Y
+#The following will be ignored if NET_USE_DHCP=N
+NET_IP_ADDRESS=192.168.10.51
+NET_BROADCAST=192.168.10.51
+NET_MASK=255.255.255.0
+NET_HOSTNAME=Novasom6_Ethernet
+NET_NETWORK=192.168.10.0
+NET_GATEWAY=192.168.10.1
+NET_DNS_LIST=192.168.10.200
+NET_DNS_DOMAIN=-
+NET_ALIAS=192.168.10.248
+NET_HTTPD_PORT=8080
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/sysconfig/system_vars SDLBase/board/novasis/NOVAsomU/Init/etc/sysconfig/system_vars
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/sysconfig/system_vars	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/sysconfig/system_vars	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1 @@
+REFERENCE_SERVER=192.168.10.132
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/sysconfig/wireless SDLBase/board/novasis/NOVAsomU/Init/etc/sysconfig/wireless
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/etc/sysconfig/wireless	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/etc/sysconfig/wireless	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,16 @@
+#Wireless
+WLAN_ENABLED=N
+WLAN_USE_DHCP=N
+WLAN_DEVICE=wlan0
+WLAN_SUFFIX=novasis.it
+#The following will be ignored if WLAN_USE_DHCP=OFF
+WLAN_IP_ADDRESS=192.168.10.199
+WLAN_BROADCAST=192.168.10.199
+WLAN_MASK=255.255.255.0
+WLAN_HOSTNAME=Novasom6_Wlan
+WLAN_NETWORK=192.168.10.0
+WLAN_GATEWAY=192.168.10.1
+WLAN_DNS_LIST=192.168.10.200
+WLAN_DNS_DOMAIN=-
+WLAN_MAC=1E:72:49:ED:F7:54
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/root/.asoundrc SDLBase/board/novasis/NOVAsomU/Init/root/.asoundrc
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/root/.asoundrc	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/root/.asoundrc	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,38 @@
+pcm.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+ctl.imxspdif {
+        type hw
+        card 0
+        channels 2
+}
+pcm.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+ctl.imxhdmisoc {
+        type hw
+        card 1
+        channels 2
+}
+pcm.copy {
+        type plug
+        slave {
+                pcm hw
+        }
+        route_policy copy
+}
+pcm.!default {
+        type hw
+        card 1
+        channels 2
+}
+ctl.!default {
+        type hw
+        card 1
+        channels 2
+}
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/root/.profile SDLBase/board/novasis/NOVAsomU/Init/root/.profile
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/root/.profile	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/root/.profile	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,10 @@
+#
+# .profile, started at boot from root user
+# 
+if [ -d /tmp/application_storage ]; then
+        if [ -f /tmp/application_storage/AutoRun.sh ]; then
+                chmod 777 /tmp/application_storage/AutoRun.sh
+                /tmp/application_storage/AutoRun.sh &
+        fi
+fi
+
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/Init/usr/share/udhcpc/default.script SDLBase/board/novasis/NOVAsomU/Init/usr/share/udhcpc/default.script
--- buildroot-2016.05/board/novasis/NOVAsomU/Init/usr/share/udhcpc/default.script	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/Init/usr/share/udhcpc/default.script	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,83 @@
+#!/bin/sh
+
+# udhcpc script edited by Tim Riker <Tim@Rikers.org>
+
+[ -z "$1" ] && echo "Error: should be called from udhcpc" && exit 1
+
+RESOLV_CONF="/etc/resolv.conf"
+[ -e $RESOLV_CONF ] || touch $RESOLV_CONF
+[ -n "$broadcast" ] && BROADCAST="broadcast $broadcast"
+[ -n "$subnet" ] && NETMASK="netmask $subnet"
+
+case "$1" in
+	deconfig)
+		/sbin/ifconfig $interface up
+		/sbin/ifconfig $interface 0.0.0.0
+
+		# drop info from this interface
+		# resolv.conf may be a symlink to /tmp/, so take care
+		TMPFILE=$(mktemp)
+		grep -vE "# $interface\$" $RESOLV_CONF > $TMPFILE
+		cat $TMPFILE > $RESOLV_CONF
+		rm -f $TMPFILE
+
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -k $interface
+		fi
+		;;
+
+	leasefail|nak)
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -wD $interface --no-chroot
+		fi
+		;;
+
+	renew|bound)
+		if [ -x /usr/sbin/avahi-autoipd ]; then
+			/usr/sbin/avahi-autoipd -k $interface
+		fi
+		/sbin/ifconfig $interface $ip $BROADCAST $NETMASK
+
+		if [ -n "$router" ] ; then
+			echo "deleting routers"
+			while route del default gw 0.0.0.0 dev $interface 2> /dev/null; do
+				:
+			done
+
+			for i in $router ; do
+				route add default gw $i dev $interface
+			done
+		fi
+
+		# drop info from this interface
+		# resolv.conf may be a symlink to /tmp/, so take care
+		TMPFILE=$(mktemp)
+		grep -vE "# $interface\$" $RESOLV_CONF > $TMPFILE
+		cat $TMPFILE > $RESOLV_CONF
+		rm -f $TMPFILE
+
+		# prefer rfc3359 domain search list (option 119) if available
+		if [ -n "$search" ]; then
+			search_list=$search
+		elif [ -n "$domain" ]; then
+			search_list=$domain
+		fi
+
+		[ -n "$search_list" ] &&
+			echo "search $search_list # $interface" >> $RESOLV_CONF
+
+		for i in $dns ; do
+			echo adding dns $i
+			echo "nameserver $i # $interface" >> $RESOLV_CONF
+		done
+                /etc/init.d/afterlease_services
+		;;
+esac
+
+HOOK_DIR="$0.d"
+for hook in "${HOOK_DIR}/"*; do
+    [ -f "${hook}" -a -x "${hook}" ] || continue
+    "${hook}" "${@}"
+done
+
+exit 0
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/post-image.sh SDLBase/board/novasis/NOVAsomU/post-image.sh
--- buildroot-2016.05/board/novasis/NOVAsomU/post-image.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/post-image.sh	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,9 @@
+#!/bin/bash
+
+echo "###### Creating uInitrd for NOVAsomU ######"
+output/host/usr/bin/mkimage -A arm -O linux -T ramdisk -C none -n "NOVAsomU" -a 0x80000 -d output/images/rootfs.ext3.gz output/images/uInitrd
+pushd ../../Deploy
+rm uInitrd
+ln -s ${BASE_DIR}/images/uInitrd
+popd
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/pre-image.sh SDLBase/board/novasis/NOVAsomU/pre-image.sh
--- buildroot-2016.05/board/novasis/NOVAsomU/pre-image.sh	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/pre-image.sh	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,9 @@
+#!/bin/bash
+
+echo "###### Customizing file system ######"
+echo "cp -r board/novasis/NOVAsomU/Init/* ${TARGET_DIR}"
+cp -r board/novasis/NOVAsomU/Init/* ${TARGET_DIR}
+chmod 777 ${TARGET_DIR}/bin/*
+chmod 777 ${TARGET_DIR}/etc/init.d/*
+sync
+exit $?
diff -Naur buildroot-2016.05/board/novasis/NOVAsomU/readme.txt SDLBase/board/novasis/NOVAsomU/readme.txt
--- buildroot-2016.05/board/novasis/NOVAsomU/readme.txt	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/board/novasis/NOVAsomU/readme.txt	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,64 @@
+***************************
+Freescale i.MX6UL EVK board
+***************************
+
+This file documents the Buildroot support for the Freescale i.MX6UL EVK board.
+
+Please read the i.MX6UL Evaluation Kit Quick Start Guide [1] for an
+introduction to the board.
+
+Build
+=====
+
+First, configure Buildroot for your i.MX6UL EVK board:
+
+  make freescale_imx6ulevk_defconfig
+
+Build all components:
+
+  make
+
+You will find in ./output/images/ the following files:
+  - imx6ul-14x14-evk.dtb
+  - rootfs.ext2
+  - rootfs.tar
+  - sdcard.img
+  - u-boot.imx
+  - zImage
+
+Create a bootable microSD card
+==============================
+
+To determine the device associated to the microSD card have a look in the
+/proc/partitions file:
+
+  cat /proc/partitions
+
+Buildroot prepares a bootable "sdcard.img" image in the output/images/
+directory, ready to be dumped on a microSD card. Launch the following
+command as root:
+
+  dd if=./output/images/sdcard.img of=/dev/<your-microsd-device>
+
+*** WARNING! This will destroy all the card content. Use with care! ***
+
+For details about the medium image layout, see the definition in
+board/freescale/imx6ulevk/genimage.cfg.
+
+Boot the i.MX6UL EVK board
+=========================
+
+To boot your newly created system (refer to the i.MX6UL EVK Quick Start Guide
+[1] for guidance):
+- insert the microSD card in the microSD slot of the board;
+- verify that your i.MX6UL EVK board jumpers and switches are set as mentioned
+  in the i.MX6UL EVK Quick Start Guide [1];
+- put a micro USB cable into the Debug USB Port and connect using a terminal
+  emulator at 115200 bps, 8n1;
+- power on the board.
+
+Enjoy!
+
+References
+==========
+[1] http://cache.freescale.com/files/32bit/doc/quick_start_guide/IMX6ULTRALITEQSG.pdf
diff -Naur -x gcc buildroot-2016.05/configs/novasomp_defconfig SDLBase/configs/novasomp_defconfig
--- buildroot-2016.05/configs/novasomp_defconfig	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/configs/novasomp_defconfig	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,108 @@
+BR2_ARCH_HAS_MMU_OPTIONAL=y
+BR2_arm=y
+BR2_ARCH="arm"
+BR2_ENDIAN="LITTLE"
+BR2_GCC_TARGET_ABI="aapcs-linux"
+BR2_GCC_TARGET_CPU="cortex-a9"
+BR2_GCC_TARGET_FPU="vfpv3-d16"
+BR2_GCC_TARGET_FLOAT_ABI="hard"
+BR2_GCC_TARGET_MODE="arm"
+BR2_BINFMT_SUPPORTS_SHARED=y
+BR2_BINFMT_ELF=y
+BR2_ARM_CPU_HAS_NEON=y
+BR2_ARM_CPU_MAYBE_HAS_NEON=y
+BR2_ARM_CPU_MAYBE_HAS_VFPV2=y
+BR2_ARM_CPU_HAS_VFPV2=y
+BR2_ARM_CPU_MAYBE_HAS_VFPV3=y
+BR2_ARM_CPU_HAS_VFPV3=y
+BR2_ARM_CPU_HAS_ARM=y
+BR2_ARM_CPU_HAS_THUMB2=y
+BR2_ARM_CPU_ARMV7A=y
+BR2_cortex_a9=y
+BR2_ARM_ENABLE_NEON=y
+BR2_ARM_ENABLE_VFP=y
+BR2_ARM_EABIHF=y
+BR2_ARM_FPU_VFPV3D16=y
+BR2_ARM_INSTRUCTIONS_ARM=y
+
+BR2_DL_DIR="/Devel/NOVAsom_SDK/Packages"
+
+BR2_TOOLCHAIN=y
+BR2_TOOLCHAIN_USES_GLIBC=y
+BR2_TOOLCHAIN_EXTERNAL=y
+BR2_TOOLCHAIN_EXTERNAL_CUSTOM=y
+BR2_TOOLCHAIN_EXTERNAL_PREINSTALLED=y
+BR2_TOOLCHAIN_EXTERNAL_PATH="/Devel/NOVAsom_SDK/Xcompiler/ArmCompiler/usr"
+BR2_TOOLCHAIN_EXTERNAL_CUSTOM_PREFIX="$(ARCH)-linux"
+BR2_TOOLCHAIN_EXTERNAL_PREFIX="$(ARCH)-linux"
+BR2_TOOLCHAIN_EXTERNAL_GLIBC=y
+BR2_TOOLCHAIN_EXTERNAL_GCC_5=y
+BR2_TOOLCHAIN_EXTERNAL_HEADERS_4_1=y
+BR2_TOOLCHAIN_EXTERNAL_CUSTOM_GLIBC=y
+BR2_TOOLCHAIN_EXTERNAL_HAS_SSP=y
+BR2_TOOLCHAIN_EXTERNAL_INET_RPC=y
+BR2_TOOLCHAIN_EXTERNAL_CXX=y
+BR2_TOOLCHAIN_EXTRA_EXTERNAL_LIBS=""
+BR2_TOOLCHAIN_HAS_NATIVE_RPC=y
+BR2_USE_WCHAR=y
+BR2_ENABLE_LOCALE=y
+BR2_INSTALL_LIBSTDCPP=y
+BR2_TOOLCHAIN_HAS_THREADS=y
+BR2_TOOLCHAIN_HAS_THREADS_DEBUG=y
+BR2_TOOLCHAIN_HAS_THREADS_NPTL=y
+BR2_TOOLCHAIN_HAS_SHADOW_PASSWORDS=y
+BR2_TOOLCHAIN_HAS_SSP=y
+BR2_ENABLE_LOCALE_PURGE=y
+BR2_ENABLE_LOCALE_WHITELIST="C en_US de fr it"
+BR2_GENERATE_LOCALE=""
+BR2_USE_MMU=y
+BR2_TARGET_OPTIMIZATION=""
+BR2_TARGET_LDFLAGS=""
+
+#
+# System configuration
+#
+BR2_TARGET_GENERIC_ISSUE="Welcome"
+BR2_TARGET_GENERIC_PASSWD_MD5=y
+BR2_TARGET_GENERIC_PASSWD_METHOD="md5"
+BR2_INIT_BUSYBOX=y
+BR2_ROOTFS_DEVICE_CREATION_DYNAMIC_DEVTMPFS=y
+BR2_ROOTFS_DEVICE_TABLE="system/device_table.txt"
+BR2_ROOTFS_SKELETON_DEFAULT=y
+BR2_TARGET_ENABLE_ROOT_LOGIN=y
+BR2_TARGET_GENERIC_ROOT_PASSWD=""
+BR2_SYSTEM_BIN_SH_BUSYBOX=y
+
+BR2_TARGET_GENERIC_GETTY_PORT="ttymxc2"
+BR2_TARGET_GENERIC_GETTY_BAUDRATE_KEEP=y
+BR2_TARGET_GENERIC_GETTY_BAUDRATE="0"
+BR2_TARGET_GENERIC_GETTY_TERM="vt100"
+BR2_TARGET_GENERIC_GETTY_OPTIONS="-l /bin/autologin.sh -n"
+BR2_TARGET_GENERIC_REMOUNT_ROOTFS_RW=y
+BR2_SYSTEM_DHCP="eth0"
+BR2_ROOTFS_POST_BUILD_SCRIPT="board/novasis/NOVAsomP/pre-image.sh"
+BR2_ROOTFS_POST_IMAGE_SCRIPT="board/novasis/NOVAsomP/post-image.sh"
+
+#
+# Filesystem images
+#
+BR2_TARGET_ROOTFS_EXT2=y
+BR2_TARGET_ROOTFS_EXT2_3=y
+BR2_TARGET_ROOTFS_EXT2_GEN=3
+BR2_TARGET_ROOTFS_EXT2_REV=1
+BR2_TARGET_ROOTFS_EXT2_LABEL=""
+BR2_TARGET_ROOTFS_EXT2_BLOCKS=0
+BR2_TARGET_ROOTFS_EXT2_INODES=0
+BR2_TARGET_ROOTFS_EXT2_EXTRA_BLOCKS=0
+BR2_TARGET_ROOTFS_EXT2_EXTRA_INODES=0
+BR2_TARGET_ROOTFS_EXT2_RESBLKS=0
+BR2_TARGET_ROOTFS_EXT2_GZIP=y
+
+#
+# Host utilities
+#
+BR2_PACKAGE_HOST_E2FSPROGS=y
+BR2_PACKAGE_HOST_GENEXT2FS=y
+BR2_PACKAGE_HOST_GO_ARCH_SUPPORTS=y
+BR2_PACKAGE_HOST_MKE2IMG=y
+BR2_PACKAGE_HOST_UBOOT_TOOLS=y
diff -Naur -x gcc buildroot-2016.05/configs/novasomu_defconfig SDLBase/configs/novasomu_defconfig
--- buildroot-2016.05/configs/novasomu_defconfig	1970-01-01 01:00:00.000000000 +0100
+++ SDLBase/configs/novasomu_defconfig	2017-01-10 14:12:55.017032694 +0100
@@ -0,0 +1,85 @@
+# architecture
+BR2_arm=y
+BR2_cortex_a7=y
+
+BR2_DL_DIR="/Devel/NOVAsom_SDK/Packages"
+
+BR2_TOOLCHAIN=y
+BR2_TOOLCHAIN_USES_GLIBC=y
+BR2_TOOLCHAIN_EXTERNAL=y
+BR2_TOOLCHAIN_EXTERNAL_CUSTOM=y
+BR2_TOOLCHAIN_EXTERNAL_PREINSTALLED=y
+BR2_TOOLCHAIN_EXTERNAL_PATH="/Devel/NOVAsom_SDK/Xcompiler/ArmCompiler/usr"
+BR2_TOOLCHAIN_EXTERNAL_CUSTOM_PREFIX="$(ARCH)-linux"
+BR2_TOOLCHAIN_EXTERNAL_PREFIX="$(ARCH)-linux"
+BR2_TOOLCHAIN_EXTERNAL_GLIBC=y
+BR2_TOOLCHAIN_EXTERNAL_GCC_5=y
+BR2_TOOLCHAIN_EXTERNAL_HEADERS_4_1=y
+BR2_TOOLCHAIN_EXTERNAL_CUSTOM_GLIBC=y
+BR2_TOOLCHAIN_EXTERNAL_HAS_SSP=y
+BR2_TOOLCHAIN_EXTERNAL_INET_RPC=y
+BR2_TOOLCHAIN_EXTERNAL_CXX=y
+BR2_TOOLCHAIN_EXTRA_EXTERNAL_LIBS=""
+BR2_TOOLCHAIN_HAS_NATIVE_RPC=y
+BR2_USE_WCHAR=y
+BR2_ENABLE_LOCALE=y
+BR2_INSTALL_LIBSTDCPP=y
+BR2_TOOLCHAIN_HAS_THREADS=y
+BR2_TOOLCHAIN_HAS_THREADS_DEBUG=y
+BR2_TOOLCHAIN_HAS_THREADS_NPTL=y
+BR2_TOOLCHAIN_HAS_SHADOW_PASSWORDS=y
+BR2_TOOLCHAIN_HAS_SSP=y
+BR2_ENABLE_LOCALE_PURGE=y
+BR2_ENABLE_LOCALE_WHITELIST="C en_US de fr it"
+BR2_GENERATE_LOCALE=""
+BR2_USE_MMU=y
+BR2_TARGET_OPTIMIZATION=""
+BR2_TARGET_LDFLAGS=""
+
+#
+# System configuration
+#
+BR2_TARGET_GENERIC_ISSUE="Welcome"
+BR2_TARGET_GENERIC_PASSWD_MD5=y
+BR2_TARGET_GENERIC_PASSWD_METHOD="md5"
+BR2_INIT_BUSYBOX=y
+BR2_ROOTFS_DEVICE_CREATION_DYNAMIC_DEVTMPFS=y
+BR2_ROOTFS_DEVICE_TABLE="system/device_table.txt"
+BR2_ROOTFS_SKELETON_DEFAULT=y
+BR2_TARGET_ENABLE_ROOT_LOGIN=y
+BR2_TARGET_GENERIC_ROOT_PASSWD=""
+BR2_SYSTEM_BIN_SH_BUSYBOX=y
+
+BR2_TARGET_GENERIC_GETTY_PORT="ttymxc0"
+BR2_TARGET_GENERIC_GETTY_BAUDRATE_KEEP=y
+BR2_TARGET_GENERIC_GETTY_BAUDRATE="0"
+BR2_TARGET_GENERIC_GETTY_TERM="vt100"
+BR2_TARGET_GENERIC_GETTY_OPTIONS="-l /bin/autologin.sh -n"
+BR2_TARGET_GENERIC_REMOUNT_ROOTFS_RW=y
+BR2_SYSTEM_DHCP="eth0"
+BR2_ROOTFS_POST_BUILD_SCRIPT="board/novasis/NOVAsomU/pre-image.sh"
+BR2_ROOTFS_POST_IMAGE_SCRIPT="board/novasis/NOVAsomU/post-image.sh"
+
+#
+# Filesystem images
+#
+BR2_TARGET_ROOTFS_EXT2=y
+BR2_TARGET_ROOTFS_EXT2_3=y
+BR2_TARGET_ROOTFS_EXT2_GEN=3
+BR2_TARGET_ROOTFS_EXT2_REV=1
+BR2_TARGET_ROOTFS_EXT2_LABEL=""
+BR2_TARGET_ROOTFS_EXT2_BLOCKS=0
+BR2_TARGET_ROOTFS_EXT2_INODES=0
+BR2_TARGET_ROOTFS_EXT2_EXTRA_BLOCKS=0
+BR2_TARGET_ROOTFS_EXT2_EXTRA_INODES=0
+BR2_TARGET_ROOTFS_EXT2_RESBLKS=0
+BR2_TARGET_ROOTFS_EXT2_GZIP=y
+
+#
+# Host utilities
+#
+BR2_PACKAGE_HOST_E2FSPROGS=y
+BR2_PACKAGE_HOST_GENEXT2FS=y
+BR2_PACKAGE_HOST_GO_ARCH_SUPPORTS=y
+BR2_PACKAGE_HOST_MKE2IMG=y
+BR2_PACKAGE_HOST_UBOOT_TOOLS=y
